# KodeGPT v0.1 — Execution Tracker

**Tracker date:** 2026-08-10  
**Repository:** `/home/sauron/dev/kodegpt`  
**Expected upstream:** `https://github.com/2ndworld/kodeGPT`  
**Blueprint authority:** KodeGPT v0.1 Master Product, Architecture & Engineering Blueprint, 2026-08-09  
**Execution plan authority:** Audited Implementation Plan V3 embedded in the Master Blueprint

> This file is a living status ledger. Update it after every RED/GREEN/VERIFY/review gate. Do not use it to weaken or reinterpret a locked security decision. When this tracker conflicts with the Master Blueprint, the Master Blueprint wins.

## 1. Status semantics

| Status | Meaning |
|---|---|
| `DONE` | Implementation exists, required fresh verification is green, the relevant review gate/evidence is satisfied, and the task's required durability commit is present. |
| `IMPLEMENTED / GATE PENDING` | Code/files are present and local verification is green, but an external, review, isolation, packaging, or acceptance requirement remains. |
| `IMPLEMENTED / DURABILITY PENDING` | Code/files and local behavioral gates are green, but a required commit/push or other durability step is not yet possible from the current execution surface. |
| `IN PROGRESS` | A RED/GREEN cycle is currently open. |
| `SCAFFOLD ONLY` | Target package/crate/file shell exists but the task behavior and tests do not. This is **not** implementation credit. |
| `NOT STARTED` | No task-specific implementation exists. |
| `BLOCKED` | A locked prerequisite currently fails. Do not work around it by weakening the design. |

## 2. Current evidence snapshot

### Repository identity

- Workspace root: `/home/sauron/dev/kodegpt` — confirmed through CodexPro.
- `origin`: `https://github.com/2ndworld/kodeGPT.git` — matches the blueprint and remains unchanged.
- Foundation commit: `184b925a295cfcbacfb3d002cb7978d863bea3a2 chore: initialize isolated kodegpt monorepo`.
- Planning/tracker preservation commit: `7332ce3 docs: add v0.1 execution plan and tracker`.
- Implementation runs in linked worktree `.worktrees/kodegpt-v0.1-execution` on branch `feat/kodegpt-v0.1-execution-wt`; it is not running on `main`.
- HTTPS publication timed out on an interactive credential path. A non-interactive SSH push then published the branch using a temporary `/tmp` known-hosts file only; no credential/profile/global Git configuration was changed.
- Durable remote branch `feat/kodegpt-v0.1-execution-wt` exists as of 2026-08-09.

**Implication:** repository identity, worktree isolation, and durable remote recovery state are now suitable for long-running implementation. Branch publication is not a release/tag.

### Host/toolchain

| Requirement | Observed | Verdict |
|---|---:|---|
| Linux x86_64 | `x86_64` | PASS |
| Node >=24 | `v24.17.0` | PASS |
| pnpm 10.15.0 | `10.15.0` | PASS |
| Rust stable | `rustc 1.97.1 (2026-07-14)` | PASS |
| Bubblewrap >=0.11.2 | `/usr/local/bin/bwrap` → `0.11.2`; uid/gid `0:0`; regular file mode `0755`; mandatory functional probe suite 7/7 | PASS — empirical functional acceptance on current host |

**Bubblewrap blocker resolution:** upstream Bubblewrap tag `v0.11.2`, peeled commit `1b80120ef26a28e065e67f89bfef873f13bdd317`, was built with `support_setuid=false` and installed root-owned at `/usr/local/bin/bwrap`. Tasks 12 and 22 subsequently proved the locked namespace, `NoNewPrivs`, capability, retained-root-FD workspace bind, HOME/state isolation, network-policy, and executable-revalidation behaviors. The final guarded run freshly executed `cargo test -p kodegpt-sandbox` and passed all 7 mandatory probes; the `ENFORCED` claim is therefore functional on this acceptance host, not version-only.

### External-state isolation evidence

The final complete local RC command set was enclosed by passive Pranikah snapshots written outside the protected repository:

- before: `/tmp/kodegpt-pranikah-final3-before.json`
- after: `/tmp/kodegpt-pranikah-final3-after.json`
- compare result: `guard unchanged`

The contents/fingerprints are intentionally not copied into the repository. An earlier run correctly detected concurrent Pranikah movement and was not counted as PASS; a controlled rerun and the final complete guarded run both confirmed KodeGPT acceptance did not alter the protected repository/listener state.

### Fresh foundation verification

Executed on 2026-08-09:

```text
pnpm install --frozen-lockfile  PASS — lockfile honored; 13 workspace projects
pnpm typecheck                  PASS
pnpm test                       PASS — 2 test files / 4 tests
cargo test --workspace          PASS — compilation succeeds, 0 Rust tests
```

Evidence-quality note: Rust green currently proves only that the scaffold crates compile. It does **not** prove protocol, workspace, sandbox, audit, process, or security behavior because those implementations/tests do not exist yet.

## 3. Repo-to-blueprint structural assessment

### Present and materially implemented

- Root pnpm workspace and Cargo workspace.
- Per-package TypeScript manifests/configs.
- Rust crate manifests/stubs.
- Root Vitest `test.projects` configuration.
- `STATE_SCHEMA_VERSION = 1`, `StateEnvelope`, and future-schema rejection test.
- Recursive external-state guard supporting `tree`, `file`, and passive `tcp_listener` fingerprints.
- Guard regression proving nested file byte changes are detected.
- Lockfiles exist.

### Current implementation coverage

The earlier scaffold-only packages are now materially implemented: artifacts, audit diagnostics, declarative extensions, MCP Apps dev-console, Linux runtime packaging, and the Bubblewrap sandbox all have task-specific source plus executable verification. The original Tasks 2–25 release-candidate baseline is durable and green. The managed public-exposure portion of Tasks 20/24 has now been replaced by the approved zrok v2 path; the exact zrok candidate has fresh full local deterministic evidence plus GitHub CI PASS.

### Remaining release gates

Real ChatGPT-host observation through `zrok-public-https-query-credential` is now captured for the exact zrok candidate: discovery/tool invocation, workspace open, file read/write/edit, and foreground process execution were observed from ChatGPT, while MCP Apps rendering itself was not observed and text fallback remained usable. A final Pranikah BEFORE/AFTER isolation window around public zrok acceptance returned `guard unchanged` after concurrent Pranikah Codex/E2E activity was allowed to finish.

The zrok replacement is durably pushed on `feat/kodegpt-v0.1-execution-wt`, GitHub CI run `31444942546` on `1199de6` is green, and the core Task 24 host/isolation evidence is captured. Host evidence is intentionally granular: Apps rendering and a positive non-empty `skill.list -> skill.inspect -> skill.load` round trip remain pending until actually observed. Remaining release work is branch consolidation, final exact-release CI, and tag creation.

## 4. Task status matrix

| Task | Area | Status | Current evidence | Next irreversible gate |
|---:|---|---|---|---|
| 1 | Independent monorepo / state / isolation guard | `DONE` | foundation `184b925`; fresh install/typecheck/TS/Rust scaffold checks green; tracked-source scans clean; durable feature branch published | A1 PASS |
| 2 | Runtime schemas + exact framing | `DONE` | `fdd7e88`; exact 8 MiB UTF-8 Content-Length framing; closed TS/Rust/schema contracts; literal TS→Rust→TS fixture round-trip | A2 PASS |
| 3 | Persistent concurrent runtime | `DONE` | `ae74aca`; concurrent dispatcher; single framed stdout writer; KernelClient correlation/death fan-out; production test-RPC exclusion | A2 PASS |
| 4 | Serialized fail-closed audit | `DONE` | `3db6714`; serialized durable JSONL sink; decision-before-effect; poison-on-outcome-failure; 0600 + rotation + secret redaction | A2 PASS |
| 5 | Trusted workspace admission | `DONE` | `42a6278fa98b1df5756a4f9a49dbbb688d6d5334`; audited `system.inspect_root`; persistent device/inode identity; atomic schema-v1 0600 trust store; local-only CLI trust surface; pathname replacement regression | B1 |
| 6 | Mount topology + root FD + overlap rejection | `DONE` | `0f3359c8bbf8c83f6cfa76a0120ce8c3d6f648ff`; escaped mountinfo decoding; same-backing/ancestor/device+inode overlap rejection; retained revalidated root FD; fail-closed topology; audited lifecycle skeletons | B1 |
| 7 | Two-phase lifecycle + monotonic profile restriction | `DONE` | `5538c5087adfa280a3e16820ed7f66fdd7d67571`; TS + Rust monotonic policy; retained-FD project profile read; OPENING→READY/CLOSING lifecycle; public `ws_...` only after activate ACK; bounded retryable close | B1 |
| 8 | `openat2` + NO_XDEV boundary | `DONE` | `d6254ab`; centralized exact openat2 flags; escape/magic-link/nested-mount/race regressions; parent-FD helper; runtime functional probe + fail-closed registration; KernelClient hello propagation | B1 |
| 9 | FD-relative read/tree/search | `DONE` | `ec78253`; retained-FD file.read; directory-FD tree with 2,000-entry ceiling; UTF-8 lexical search with 200-match/256 KiB snippet ceilings; symlink non-descent; audit-before-I/O; public ws_ service keeps kc_ private | B1 |
| 10 | Connector auth + HTTP trust | `DONE` | `02d7a7c`; verifier-only schema-v1 0600 credential store; kgc_ rotation; domain-separated SHA-256 + timingSafeEqual; immediate old-token invalidation; CLI status/rotate; canonical Host/Origin + JSON/body-size HTTP trust guard | C1 |
| 11 | MCP 2026-07-28 surface | `DONE` | `51ee91d`; exact v2 tuple; modern raw HTTP/stdio 2026-07-28; locked 11-tool registry + exact annotations/schemas; trust→bearer→MCP ordering; no session/legacy path; fail-closed loopback start | C1 |
| 12 | Hardened Bubblewrap provider | `DONE` | `2f7f8b3`; trusted executable identity/revalidation; hardened root-FD Bubblewrap provider; complete functional isolation probes; private `ex_...` registry; current acceptance host uses a narrow enforced AppArmor profile attached only to trusted `/usr/local/bin/bwrap` while generic unprivileged userns remains restricted | C2 |
| 13 | Kernel raw output spool | `DONE` | `700350c`; private `ka_...` metadata; protected `artifacts/raw` root; one spool per private execution; deterministic 64 MiB combined source cap; audit-before-create/failure evidence; no serialized host path | C2 |
| 14 | Race-safe file mutation | `DONE` | `31ee853` + `30727e9`; retained-root-FD atomic write/edit; observe/allowWrite denial; traversal/symlink/cross-workspace/state self-protection; exact-count conflict; mutating MCP surface; audit-before-effect; full JS/Rust verification green | D1 |
| 15 | Hardened read-only Git | `DONE` | audited read-only `git.status`/`git.diff`; helper isolation; hardened sandbox execution; public artifact URI only; core + full-stack integration green | D1 PASS |
| 16 | Sandboxed process/cancel | `DONE` | `17b6cf5`; structured no-shell run/status/cancel; policy/cwd/env checks; opaque `op_`; tree cancellation; root-FD sandbox; registry-poison regression proves a spawned child is killed before fail-closed return; runtime/sandbox/process-policy green | PASS |
| 17 | Artifact facade/quota/TTL | `DONE` | `17b6cf5`; `artifact://ka_...` facade; internal bounded read <=1 MiB; canonical Base64 validation; symlink rejection; 24h TTL; 1 GiB aggregate reservation; active-writer protection; no host path/private IDs | PASS |
| 18 | Audit diagnostics/correlation | `DONE` | `17b6cf5`; fixed-path bounded AuditReader; public `req_`/`op_` only; secret/env/argv redaction; runtime audit suite + audit-redaction acceptance green | PASS |
| 19 | MCP Apps Dev Console | `DONE` | `17b6cf5`; ext-apps isolated to dev-console; self-contained HTML; normalized state precedence/freshness; v2 resource + `console.state`; behavioral HTTP regression proves per-request `io.modelcontextprotocol/clientCapabilities` Apps detection; non-UI fallback/conformance green | PASS |
| 20 | Declarative extensions / manual HTTPS | `DONE` | `17b6cf5`; closed data-only manifest/registry; executable fields and sibling JS execution rejected; bounded `extension.list`; HTTPS-only exact public trust; listener remains loopback/no tunnel subprocess | PASS |
| 21 | Linux packaging / clean install | `DONE` | `17b6cf5`; production runtime excludes test RPCs; package-owned runtime resolver; executable platform package; tarball/runtime SHA-256 verification; clean HOME/prefix doctor/start/discover/health/shutdown smoke green | PASS |
| 22 | Full security acceptance | `DONE` | `17b6cf5`; post-review `pnpm test:security` 34/34; Rust workspace all green with 29 runtime tests and no skipped sandbox invariant; Bubblewrap probes executed; kernel-death fail-closed + registry-poison child cleanup; no TS fallback | PASS |
| 23 | MCP/Apps conformance | `DONE` | `17b6cf5`; two-workspace full-stack lifecycle; exact 2026-07-28 transport/surface; per-request Apps capability + extension/non-UI contracts; post-review `pnpm test:integration` 20/20 and `pnpm test:acceptance` 3/3 | PASS |
| 24 | ChatGPT host + Pranikah isolation | `DONE / HOST PARTIAL` | `17b6cf5` plus 2026-08-12 host follow-up; passive repo/listener guard returned `guard unchanged`; real ChatGPT observed tool discovery, locally trusted workspace open, read, successful write/edit round trip with exact revert, process calls reaching KodeGPT including policy-side denial, and skill action exposure. Apps rendering and positive non-empty skill list/inspect/load remain separately unobserved | local F1 isolation PASS; observed action categories PASS; Apps + positive skill round trip pending |
| 25 | CI/performance/final RC | `DONE` | `17b6cf5`; Node-24 CI contract, exact Bubblewrap build, forbidden-pattern scanner, clean package gate, release checklist, rustfmt check, 5+30 record-only benchmark, 151/151 full Vitest suite, build/typecheck, protocol/security/isolation/acceptance, package smoke, and Pranikah compare all green | PASS; durability satisfied; remaining host claim categories are granular, not an all-or-nothing gate |

## 5. Task 1 closeout ledger

The foundation task is materially implemented, but close it only after the checklist below has fresh evidence.

- [x] Repository root is independent from Pranikah.
- [x] Root `package.json`, pnpm workspace, Cargo workspace, package/crate manifests exist.
- [x] Root Vitest uses `test.projects`; no deprecated `vitest.workspace.ts` is present.
- [x] `STATE_SCHEMA_VERSION = 1` exists.
- [x] Schema version `2` rejection is tested.
- [x] External-state guard fingerprints nested tree bytes, file bytes, symlink targets, and passive TCP listener state.
- [x] Nested byte-change regression is green.
- [x] `pnpm typecheck` fresh PASS.
- [x] `pnpm test` fresh PASS.
- [x] `cargo test --workspace` fresh PASS for current scaffold.
- [x] `/tmp/kodegpt-pranikah-guard.json` exists.
- [x] `/tmp/kodegpt-pranikah-before.json` exists.
- [x] Pre-documentation scan of product source/config found no `Pranikah` or `.codexpro` runtime coupling; planning docs intentionally name these forbidden external boundaries.
- [x] Confirm A1 using a tracked-source-only secret/personal-identifier scan after documentation changes.
- [x] Publish a durable feature branch so implementation history is recoverable remotely.
- [x] Record A1 review result in this tracker.

### Task 1 evidence record

```text
Task: 1 — Independent Monorepo / State / Isolation Guard
Status: DONE
Commit(s): 184b925a295cfcbacfb3d002cb7978d863bea3a2; 7332ce3 (plan/tracker preservation before closeout)
RED command + expected failure: historical Task 1 RED is already embodied by the existing foundation; closeout intentionally reverified rather than rewriting correct baseline behavior
GREEN command + result: pnpm typecheck PASS; pnpm test PASS (4/4); cargo test --workspace PASS (0 Rust tests, scaffold compile only)
Full task verification command + result: pnpm install --frozen-lockfile PASS; pnpm typecheck PASS; pnpm test PASS; cargo test --workspace PASS
Security regression(s): recursive external-state guard nested-byte regression remains green
Changed files reviewed: .gitignore; docs/superpowers/plans/2026-08-09-kodegpt-v0.1-detailed-execution-plan.md; docs/implementation/v0.1-execution-tracker.md
Blueprint requirements covered: independent repo/state namespace; versioned state; recursive isolation guard; no runtime CodexPro/Pranikah coupling; durable branch state
Known deviations: none
External prerequisites checked: Bubblewrap /usr/local/bin/bwrap 0.11.2, root-owned regular mode 0755; functional sandbox acceptance deliberately deferred to Tasks 12/22
Gate decision: PASS (A1)
Reviewed at: 2026-08-09 Asia/Jakarta
```

### Task 2 evidence record

```text
Task: 2 — Canonical Runtime Schemas and Exact Content-Length Framing
Status: DONE
Commit(s): fdd7e88 feat(protocol): define framed runtime contract
RED command + expected failure: pnpm --filter @kodegpt/protocol test → FAIL because ./frame.js did not exist; cargo test -p kodegpt-protocol → FAIL because codec/types/dependencies did not exist; pnpm test:protocol → FAIL before TS runtime types/index and again before JSON schemas/cross-language helper existed; dedicated Rust regression reproduced inheritEnv=true being accepted before the type-level fix
GREEN command + result: pnpm --filter @kodegpt/protocol test PASS (14/14); cargo test -p kodegpt-protocol PASS (8/8); pnpm test:protocol PASS (9/9)
Full task verification command + result: pnpm --filter @kodegpt/protocol typecheck PASS; all three Task 2 verification commands PASS; pnpm typecheck PASS across workspace packages; pnpm test PASS (27/27); cargo test --workspace PASS
Security regression(s): >8 MiB declared body rejected before body allocation; exact Content-Length syntax only; duplicate/non-decimal/lowercase/LF-only variants rejected; truncated/malformed body rejected; unknown top-level/params rejected; inheritEnv=true rejected in TS/Rust/schema; legacy initialize/session concepts absent from production protocol
Changed files reviewed: packages/protocol; crates/protocol; schemas/runtime; tests/fixtures/runtime; tests/protocol; Cargo.lock; pnpm-lock.yaml
Blueprint requirements covered: UTF-8 byte-count framing; MAX_FRAME_BYTES=8 MiB; malformed-frame rejection; bounded decoder body allocation; closed security-sensitive request types; Draft 2020-12 closed schemas; semantic TS→Rust→TS fixture parity; no legacy MCP/session transport concepts in internal protocol
Known deviations: no architecture deviation; FrameDecoder.finish() is an additive EOF/truncation check needed by the locked truncated-body test; a separate reviewer subagent was unavailable through this bridge, so staged unified diff review plus fresh full verification was used without claiming independent subagent review
External prerequisites checked: Node/pnpm and Rust toolchains only; Bubblewrap is not exercised by Task 2
Gate decision: Task 2 PASS; A2 NOT YET REVIEWED because Tasks 3–4 remain
Reviewed at: 2026-08-09 Asia/Jakarta
```

### Task 3 evidence record

```text
Task: 3 — Concurrent Persistent Runtime and Production-Safe Test Harness
Status: DONE
Commit(s): ae74aca feat(runtime): add concurrent persistent kernel
RED command + expected failure: cargo test -p kodegpt-runtime --features runtime-test-methods dispatcher → FAIL because run_dispatcher did not exist; pnpm --filter @kodegpt/core test → FAIL because kernel-client.js did not exist
GREEN command + result: cargo test -p kodegpt-runtime --features runtime-test-methods dispatcher PASS (2/2); pnpm --filter @kodegpt/core test PASS (6/6 including state-version baseline, 5 KernelClient runtime tests)
Full task verification command + result: exact Task 3 commands PASS; pnpm --filter @kodegpt/core typecheck PASS; pnpm typecheck PASS across workspace; pnpm test PASS (32/32); cargo test --workspace PASS with default features; production runtime built/tested without runtime-test-methods and returns METHOD_NOT_FOUND for test RPCs
Security regression(s): runtime.hello remains responsive while 500 ms request is pending; delayed responses correlate strictly by ID; SIGKILL rejects all pending calls with RUNTIME_UNAVAILABLE; feature build still requires explicit enableTestMethods; production build cannot execute test RPC behavior; runtime source scan found no print!/println! protocol pollution; TS has no runtime-failure filesystem/process fallback
Changed files reviewed: crates/runtime; packages/core; packages/protocol/package.json export boundary; Cargo.lock; pnpm-lock.yaml
Blueprint requirements covered: blocking framed reader → Tokio request channel → concurrent per-request tasks → one response channel → exactly one framed stdout writer; persistent KernelClient lifecycle/correlation; runtime-death fan-out; feature-gated test harness disabled by default; protocol package consumed through explicit workspace dependency
Known deviations: no architecture deviation; optional cargo fmt --check could not run because rustfmt is not installed on the host stable toolchain, and the host toolchain was not mutated because rustfmt is not a locked execution prerequisite
External prerequisites checked: Rust/Node toolchains compile and run; no Bubblewrap/Pranikah dependency used by Task 3
Gate decision: Task 3 PASS; A2 NOT YET REVIEWED because Task 4 remains
Reviewed at: 2026-08-09 Asia/Jakarta
```

### Task 4 evidence record

```text
Task: 4 — Durable Serialized Audit Foundation
Status: DONE
Commit(s): 3db6714 feat(audit): add durable fail-closed security log
RED command + expected failure: cargo test -p kodegpt-runtime audit → FAIL because AuditSink/typed audit API did not exist; pnpm exec vitest run tests/security/audit-foundation.test.ts → 5/5 FAIL because auditHealthy/test.audit_effect/schema were absent
GREEN command + result: cargo test -p kodegpt-runtime audit PASS (5/5); pnpm exec vitest run tests/security/audit-foundation.test.ts PASS (6/6 after symlink hardening regression)
Full task verification command + result: pnpm typecheck PASS; pnpm test PASS (38/38); cargo test --workspace PASS (8 protocol + 5 audit Rust tests); cargo test -p kodegpt-runtime --features runtime-test-methods PASS (7/7); pnpm test:protocol PASS (9/9)
Security regression(s): 100 concurrent decision appends produce exactly 100 independently parseable JSONL records; audit init failure keeps runtime.hello diagnostics available but blocks test OS effect; injected decision failure proves marker absent; injected outcome failure poisons sink and blocks later effect; audit mode 0600; default rotation contract 10 MiB / 5 retained files; invalid identifier payloads are sanitized; runtime-built SUPER/_SECRET/_MARKER/_123 marker has zero serialized occurrences; pre-existing marker symlink is rejected without touching target
Changed files reviewed: crates/runtime/src/audit.rs; crates/runtime/src/dispatcher.rs; crates/runtime/src/main.rs; packages/core/src/kernel-client.ts; schemas/runtime/audit.schema.json; tests/security/audit-foundation.test.ts
Blueprint requirements covered: single serialized writer; durable decision/outcome APIs; decision durable before OS effect; fail-closed poison state; typed/redacted non-secret metadata; diagnostic health exposure; feature-gated test fault injection; bounded rotation; protocol-only stdout retained
Known deviations: no architecture deviation; test.audit_effect is intentionally feature-gated and restricted to create_new direct children of test state root to avoid becoming a general write primitive
External prerequisites checked: local filesystem durability primitives and runtime feature harness only; no Bubblewrap dependency exercised
Gate decision: PASS (A2) after fresh independent Tasks 2–4 verification and runtime stdout source scan
Reviewed at: 2026-08-09 Asia/Jakarta
```

### Gate A2 review record

```text
Gate: A2 — Protocol / Persistent Runtime / Fail-Closed Audit
Decision: PASS
Fresh Task 2 evidence: pnpm --filter @kodegpt/protocol test PASS (14/14); cargo test -p kodegpt-protocol PASS (8/8); pnpm test:protocol PASS (9/9)
Fresh Task 3 evidence: pnpm --filter @kodegpt/core test PASS (6/6); cargo test -p kodegpt-runtime --features runtime-test-methods dispatcher PASS (2/2)
Fresh Task 4 evidence: cargo test -p kodegpt-runtime audit PASS (5/5); audit e2e PASS (6/6); full pnpm test PASS (38/38); full cargo workspace PASS
Protocol-only stdout check: crates/runtime/src has zero print!/println! call sites; framed writer remains the only stdout writer
Reusable authority check: later OS handlers can consume the same Arc<AuditSink> decision()/outcome() authority and observe poisoned health without adding a second audit writer
Reviewer limitation: no separate subagent reviewer available through this bridge; gate uses staged unified-diff review plus fresh independent command evidence without claiming independent-agent review
```

### Task 5 evidence record

```text
Task: 5 — Trusted Workspace Admission with Persistent Filesystem Identity
Status: DONE
Commit(s): 42a6278fa98b1df5756a4f9a49dbbb688d6d5334 feat(trust): persist local workspace identity
RED command + expected failure: cargo test -p kodegpt-workspace-io identity → FAIL initially because inspect_root did not exist; cargo test -p kodegpt-runtime --features runtime-test-methods inspect_root → FAIL initially because system.inspect_root was METHOD_NOT_FOUND/absent; pnpm --filter @kodegpt/trust test → FAIL initially because workspace-trust-store.js did not exist; pnpm --filter kodegpt test → FAIL initially because @kodegpt/trust was unavailable
GREEN command + result: cargo test -p kodegpt-workspace-io identity PASS (3/3); cargo test -p kodegpt-runtime inspect_root PASS (2/2); pnpm --filter @kodegpt/trust test PASS (4/4); pnpm --filter kodegpt test PASS (2/2)
Full task verification command + result: pnpm typecheck PASS; pnpm test PASS (45/45 across 10 files); cargo test --workspace PASS (8 protocol, 7 default-feature runtime, 3 workspace-io tests; remaining scaffold crates/doc-tests green)
Security regression(s): real runtime/KernelClient E2E replaces a trusted directory with a new directory at the identical canonical pathname and requireTrusted rejects the changed device/inode identity with WORKSPACE_IDENTITY_CHANGED; system.inspect_root returns identity only and never capability; equal/ancestor/descendant KodeGPT-state overlap returns WORKSPACE_STATE_OVERLAP; MCP source scan found zero trust/untrust/inspect_root/inspectRoot surface; crates/runtime/src scan found zero print!/println! call sites
Changed files reviewed: Cargo.lock; apps/cli/package.json; apps/cli/src/commands/workspace.test.ts; apps/cli/src/commands/workspace.ts; crates/runtime/Cargo.toml; crates/runtime/src/audit.rs; crates/runtime/src/dispatcher.rs; crates/workspace-io/Cargo.toml; crates/workspace-io/src/identity.rs; crates/workspace-io/src/lib.rs; packages/trust/package.json; packages/trust/src/index.ts; packages/trust/src/workspace-trust-store.test.ts; packages/trust/src/workspace-trust-store.ts; pnpm-lock.yaml; schemas/runtime/audit.schema.json; tests/security/workspace-trust.test.ts
Blueprint requirements covered: canonical display root plus persistent device-major/device-minor/inode trust identity; audited inspect before filesystem inspection; no capability from inspect RPC; state-root overlap rejection; schemaVersion 1 trust storage; explicit future-schema rejection; atomic 0600 file with 0700 trust directory and fsync/rename/directory-sync durability; local-only trust/untrust/list; default observe ceiling; no MCP trust admission
Known deviations: no architecture deviation. Mount ID is not persisted and mount topology/root-FD authority are intentionally deferred to Task 6. Independent reviewer subagent is unavailable through the current CodexPro action surface, so review used full staged unified diff + fresh targeted/full verification + explicit security-source scans without claiming independent reviewer coverage. Optional rustfmt remains unavailable on the host stable toolchain and was not installed because it is not a locked execution prerequisite.
External prerequisites checked: linked worktree and branch feat/kodegpt-v0.1-execution-wt confirmed; origin remains https://github.com/2ndworld/kodeGPT.git; no Pranikah or CodexPro runtime dependency added
Gate decision: Task 5 PASS; B1 NOT YET REVIEWED because Tasks 6–9 remain
Reviewed at: 2026-08-09 Asia/Jakarta
```

### Task 6 evidence record

```text
Task: 6 — Mount Topology Normalization, Root FD Authority, Alias/Overlap Rejection
Status: DONE
Commit(s): 0f3359c8bbf8c83f6cfa76a0120ce8c3d6f648ff feat(security): reject overlapping workspace backing trees
RED command + expected failure: cargo test -p kodegpt-workspace-io mountinfo → FAIL because parse_mountinfo/backing_tree_for_path did not exist; cargo test -p kodegpt-workspace-io registry → FAIL because WorkspaceRegistry/WorkspaceRegistryError/roots_overlap did not exist; cargo test -p kodegpt-runtime workspace → FAIL because workspace.register/lifecycle dispatch returned no capability/METHOD_NOT_FOUND; pnpm exec vitest run tests/protocol/runtime-schema.test.ts → FAIL because begin_close/cancel_executions/unregister were absent. Review-driven RED also reproduced a valid host nsfs mountinfo line being rejected, and malformed \\777 input panicking on u8 overflow before both parser defects were fixed fail-closed.
GREEN command + result: cargo test -p kodegpt-workspace-io registry PASS (5/5); cargo test -p kodegpt-workspace-io mountinfo PASS (5/5); cargo test -p kodegpt-runtime workspace PASS (2/2)
Full task verification command + result: pnpm typecheck PASS; pnpm test PASS (46/46 across 10 files); cargo test --workspace PASS (8 protocol, 9 default-feature runtime, 13 workspace-io; remaining crates/doc-tests green); cargo test -p kodegpt-runtime --features runtime-test-methods PASS (11/11); protocol parity/schema coverage green in the full suite
Security regression(s): mountinfo escaped-space/backslash decoding; bind aliases normalize to one backing tree; backing ancestor/descendant rejected both directions; exact device+inode alias dimension rejected; unavailable/malformed topology issues no capability and maps to MOUNT_TOPOLOGY_UNAVAILABLE; valid opaque nsfs roots parse but cannot become backing-path authority; retained root FD still identifies the original directory after visible pathname replacement; FD identity is revalidated after open; audit-decision fault against a missing root returns AUDIT_UNAVAILABLE before root inspection; duplicate runtime registration returns WORKSPACE_ROOT_OVERLAP; no production print!/println! in crates/runtime/src; MCP scan found no trust/inspect/register lifecycle surface
Changed files reviewed: crates/protocol/src/lib.rs; crates/protocol/src/types.rs; crates/protocol/tests/protocol_contract.rs; crates/runtime/src/audit.rs; crates/runtime/src/dispatcher.rs; crates/workspace-io/src/identity.rs; crates/workspace-io/src/lib.rs; crates/workspace-io/src/mountinfo.rs; crates/workspace-io/src/registry.rs; packages/protocol/src/runtime-types.ts; schemas/runtime/audit.schema.json; schemas/runtime/request.schema.json; tests/fixtures/runtime/workspace.begin_close.json; tests/fixtures/runtime/workspace.cancel_executions.json; tests/fixtures/runtime/workspace.unregister.json; tests/protocol/framing-parity.test.ts; tests/protocol/runtime-schema.test.ts
Blueprint requirements covered: registration-time /proc/self/mountinfo parsing with kernel escape handling; current backing-tree normalization used only for overlap detection; visible canonical-path overlap + same-device normalized-backing overlap + exact persistent dev/inode alias rejection; mount ID is neither required nor persisted; root directory opened with O_DIRECTORY|O_NOFOLLOW|O_CLOEXEC and retained as OwnedFd; persistent identity revalidated from FD metadata; capability issued only after all registration checks; mount topology fail-closed; lifecycle RPC skeleton set is canonical across TS/Rust/schema and audited before registry effects
Known deviations: no architecture deviation. Task 6 lifecycle methods are intentionally skeletal: monotonic restriction, readiness enforcement, and bounded close belong to Task 7. openat2 and all FD-relative user path operations remain Task 8/9. The pre-existing Task 5 identity replacement unit was stabilized by retaining the old directory FD because this filesystem can immediately reuse an inode after unlink; production persistent identity semantics were not changed. Independent reviewer subagent remains unavailable through the current CodexPro action surface, so review used staged/full unified diff, security-specific source review, fresh targeted/full verification, and explicit invariant checks without claiming independent reviewer coverage. Optional rustfmt remains unavailable on the host stable toolchain.
External prerequisites checked: real host /proc/self/mountinfo successfully parses and the covering temp-directory mount device matches stat device; linked worktree/feature branch and independent KodeGPT architecture remain unchanged; no Pranikah or CodexPro runtime dependency added
Gate decision: Task 6 PASS; B1 NOT YET REVIEWED because Tasks 7–9 remain
Reviewed at: 2026-08-09 Asia/Jakarta
```

### Task 7 evidence record

```text
Task: 7 — Two-Phase Activation and Monotonic Project Restriction
Status: DONE
Commit(s): 5538c5087adfa280a3e16820ed7f66fdd7d67571 feat(workspace): activate monotonic trusted workspaces
RED command + expected failure: pnpm --filter @kodegpt/profiles test → FAIL because the profiles implementation/index did not exist; cargo test -p kodegpt-policy → FAIL because the Rust policy authority/protocol dependency did not exist; cargo test -p kodegpt-runtime workspace_policy_restriction_cannot_widen_after_narrowing → FAIL because the Task 6 skeleton accepted widening; pnpm --filter @kodegpt/core test → FAIL because WorkspaceManager/profile dependencies did not exist; cargo test -p kodegpt-runtime workspace_project_profile_read_uses_retained_root_fd_after_path_replacement → FAIL because workspace.read_project_profile was absent; review-driven RED proved WorkspaceManager incorrectly published READY when workspace.activate returned {ok:false} before the acknowledgement gate was added
GREEN command + result: pnpm --filter @kodegpt/profiles test PASS (4/4); pnpm --filter @kodegpt/core test PASS (11/11); cargo test -p kodegpt-runtime workspace PASS (4/4); dedicated Rust policy tests PASS (3/3); workspace-io registry lifecycle PASS (6/6); hard-coded retained-FD project profile read PASS (2/2)
Full task verification command + result: pnpm typecheck PASS; pnpm test PASS (55/55 across 12 files); cargo test --workspace PASS (3 policy, 8 protocol, 11 default-feature runtime, 16 workspace-io; remaining crates/doc-tests green); cargo test -p kodegpt-runtime --features runtime-test-methods PASS (13/13); cargo test -p kodegpt-protocol PASS (8/8); pnpm test:protocol PASS (10/10)
Security regression(s): observe ceiling rejects trusted/write/process escalation; develop restriction can disable write but cannot later widen; executable/env allowlists only narrow by subset; Rust repeats monotonic enforcement so TS is not the final security authority; workspace.read_project_profile is audited before its OS read and uses retained root FD after visible-path replacement; hard-coded profile reader uses openat2 BENEATH|NO_MAGICLINKS|NO_XDEV with no pathname fallback and rejects outside symlink; OPENING/CLOSING public lookup returns WORKSPACE_NOT_READY; malformed activate acknowledgement cannot publish READY and triggers capability cleanup; close uses one monotonic <=5 second deadline, transitions to non-ready before await, rejects concurrent close, reports WORKSPACE_CLOSE_INCOMPLETE on timeout, and supports safe retry while remaining CLOSING; production runtime source has zero print!/println! call sites; MCP source scan found no trust/inspect/profile/lifecycle surface
Changed files reviewed: Cargo.lock; crates/policy/Cargo.toml; crates/policy/src/lib.rs; crates/protocol/src/types.rs; crates/protocol/tests/protocol_contract.rs; crates/runtime/Cargo.toml; crates/runtime/src/audit.rs; crates/runtime/src/dispatcher.rs; crates/workspace-io/Cargo.toml; crates/workspace-io/src/lib.rs; crates/workspace-io/src/profile.rs; crates/workspace-io/src/registry.rs; packages/core/package.json; packages/core/src/workspace-manager.test.ts; packages/core/src/workspace-manager.ts; packages/profiles/package.json; packages/profiles/src/index.ts; packages/profiles/src/presets.ts; packages/profiles/src/resolve-profile.test.ts; packages/profiles/src/resolve-profile.ts; packages/profiles/src/schema.ts; packages/protocol/src/runtime-types.ts; pnpm-lock.yaml; schemas/runtime/audit.schema.json; schemas/runtime/request.schema.json; tests/fixtures/runtime/workspace.read_project_profile.json; tests/protocol/framing-parity.test.ts
Blueprint requirements covered: observe/develop/trusted profile model; shared TS protocol schema as profile schema source; trusted ceiling may only narrow; independent Rust monotonic policy authority; dedicated internal project-profile read after private registration; public ws_ mapping only after activation succeeds; OPENING/READY/CLOSING state enforcement; private kc_ capability never returned by public WorkspaceManager; bounded close with cancel/unregister and explicit incomplete error
Known deviations: no architecture deviation. Task 7 uses openat2 only for the hard-coded internal .kodegpt/profile.json read because Node/pathname reading after registration would violate retained-root-FD authority. This is intentionally not counted as Task 8: reusable/general path validation, absolute/.. rejection, magic-link/nested-mount/race tests, and general FILESYSTEM_BOUNDARY_UNAVAILABLE semantics remain Task 8. Exact preset field defaults beyond the locked profile names/schema are not specified in the repository execution plan; TS product defaults are therefore explicit local defaults while Rust authority evaluates the actual complete policy fields and monotonic relation rather than trusting a profile name alone. Independent reviewer subagent remains unavailable through the current CodexPro action surface, so review used unstaged + staged unified diff, review-driven RED regressions, fresh targeted/full verification, and explicit boundary/MCP/stdout scans without claiming independent reviewer coverage. Optional rustfmt remains unavailable on the host stable toolchain.
External prerequisites checked: host kernel supports the internal rustix openat2 call with required resolution flags; no Pranikah or CodexPro runtime dependency added; trust remains local-only and MCP remains without trust/lifecycle admission
Gate decision: Task 7 PASS; B1 NOT YET REVIEWED because Tasks 8–9 remain
Reviewed at: 2026-08-09 Asia/Jakarta
```

### Task 8 evidence record

```text
Task: 8 — FD-Relative openat2 Boundary with NO_XDEV
Status: DONE
Commit(s): d6254ab feat(files): enforce fd-relative no-xdev path boundary
RED command + expected failure: cargo test -p kodegpt-workspace-io openat → FAIL initially because OpenatBoundaryError/open_existing_beneath/open_parent_beneath did not exist; cargo test -p kodegpt-runtime unavailable_filesystem_boundary_is_reported_and_blocks_registration → FAIL initially because run_dispatcher_with_boundary_status did not exist; pnpm --filter @kodegpt/core test → FAIL initially because KernelHello dropped filesystemBoundaryAvailable and the asserted value was undefined. These RED observations predate the resumed session and were preserved from the interrupted local Task 8 cycle rather than reconstructed from remote.
GREEN command + result: cargo test -p kodegpt-workspace-io openat PASS (4/4 after finishing parent-symlink, parent nested-mount, and functional-probe coverage); cargo test -p kodegpt-workspace-io profile PASS (2/2); cargo test -p kodegpt-runtime unavailable_filesystem_boundary_is_reported_and_blocks_registration PASS (1/1); pnpm --filter @kodegpt/core test PASS (11/11)
Full task verification command + result: pnpm typecheck PASS; pnpm test PASS (55/55 across 12 files); cargo test --workspace PASS (3 policy, 8 protocol, 12 default-feature runtime, 20 workspace-io; remaining crates/doc-tests green); cargo test -p kodegpt-runtime --features runtime-test-methods PASS (14/14)
Security regression(s): open_existing_beneath rejects parent traversal, absolute paths, outside symlink, procfs magic-link resolution, and real nested-mount crossing; symlink-swap race performs thousands of attempts and never returns outside content; open_parent_beneath uses the same exact retained-FD boundary and rejects outside-symlink parent plus /proc mount crossing; functional probe verifies actual NO_XDEV semantics instead of kernel-version inference; unavailable semantics are reported in runtime.hello and workspace.register fails closed with FILESYSTEM_BOUNDARY_UNAVAILABLE before capability issuance; profile read now consumes the centralized helper; direct openat2 search under crates found only crates/workspace-io/src/openat.rs; no canonicalize-then-open fallback exists in the boundary/profile path; production crates scan found zero print!/println! call sites; packages/mcp-server scan found no workspace.* or trust surface
Changed files reviewed: crates/runtime/src/dispatcher.rs; crates/workspace-io/src/lib.rs; crates/workspace-io/src/openat.rs; crates/workspace-io/src/profile.rs; packages/core/src/kernel-client.test.ts; packages/core/src/kernel-client.ts
Blueprint requirements covered: reusable retained-root-FD path authority; exact RESOLVE_BENEATH|RESOLVE_NO_MAGICLINKS|RESOLVE_NO_XDEV flags centralized in one module; non-empty relative path validation; no create/truncate through existing-open helper; retained parent-FD helper for later mutation; functional capability probe; fail-closed runtime registration when required semantics are unavailable; no canonicalize-then-open fallback; existing Task 7 project-profile read refactored onto the centralized authority
Known deviations: no architecture deviation. Task 8 does not implement general read/tree/search; that remains Task 9 and Gate B1 therefore remains pending. The three final coverage additions resumed from the interrupted Task 8 implementation and were expected to pass against the already-written local production code; the original RED evidence above remains the authoritative TDD history and was not overwritten. Independent reviewer subagent remains unavailable through the current CodexPro action surface, so review used unstaged + staged unified diff, fresh targeted/full verification, and explicit security scans without claiming independent reviewer coverage. Optional rustfmt remains unavailable on the host stable toolchain.
External prerequisites checked: linked worktree confirmed by distinct git-dir/common-dir; branch feat/kodegpt-v0.1-execution-wt confirmed; origin remains https://github.com/2ndworld/kodeGPT.git; host functional boundary probe PASS; no Pranikah or CodexPro runtime dependency added
Gate decision: Task 8 PASS; B1 NOT YET REVIEWED because Task 9 remains
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Task 9 evidence record

```text
Task: 9 — FD-Relative Read, Tree, and Lexical Search
Status: DONE
Commit(s): ec78253 feat(files): add fd-relative audited workspace reads
RED command + expected failure: cargo test -p kodegpt-workspace-io read → compile FAIL because the new read/tree/search APIs and limits did not exist; pnpm test:protocol → FAIL because file.tree/file.search were absent from the closed TS/Rust/schema runtime method contract; cargo test -p kodegpt-runtime file_operations_use_retained_root_fd_and_fail_before_io_when_audit_is_unavailable → FAIL because file.read still returned METHOD_NOT_FOUND/result.contents was null; pnpm --filter @kodegpt/core test → FAIL because WorkspaceManager.readFile did not exist. During GREEN debugging, the first directory iterator attempt using a cloned retained directory FD exposed a shared-open-file-description offset bug: repeated_tree_calls_do_not_consume_the_retained_root_directory_offset failed with first=[a.txt], second=[]; the regression was kept and the implementation changed to open a fresh directory FD through the centralized openat2 authority. An earlier libc directory-iteration attempt also failed compilation under #![forbid(unsafe_code)]; the lint/security boundary was not weakened and the implementation was replaced by rustix RawDir.
GREEN command + result: cargo test -p kodegpt-workspace-io read PASS (8/8 selected by filter, including file read escape/nested-mount rejection, symlink non-descent, repeated-tree FD-offset regression, deterministic 2,000-entry ceiling, binary skip, 200-match ceiling, and 256 KiB snippet ceiling); cargo test -p kodegpt-runtime file_operations_use_retained_root_fd_and_fail_before_io_when_audit_is_unavailable PASS (1/1); pnpm --filter @kodegpt/core test PASS (12/12); pnpm test:protocol PASS (10/10); tests/security/workspace-read-boundary.test.ts PASS (2/2)
Full task verification command + result: pnpm typecheck PASS across 12 TypeScript workspace projects; pnpm test PASS (58/58 across 13 files); cargo test --workspace PASS (3 policy, 8 protocol, 13 default-feature runtime, 26 workspace-io; remaining crates/doc-tests green); cargo test -p kodegpt-runtime --features runtime-test-methods PASS (15/15)
Security regression(s): file.read is bounded to 1 MiB and rejects parent traversal, outside symlink, and real nested-mount crossing via retained-root openat2; tree traversal uses directory FDs as the recursion frontier, lists but never descends directory symlinks, uses fresh openat2 directory FDs so the retained root offset is not consumed, and is bounded to 2,000 deterministic entries; search reopens candidate files beneath the retained root, skips binary/NUL and invalid UTF-8 files, limits each searched file to 1 MiB, caps output at 200 matches and 256 KiB aggregate snippets, and safely skips boundary/not-found race outcomes; runtime visible-path replacement regression proves read/tree/search still see the originally registered retained root; every file RPC passes through durable audit decision before the registry/filesystem operation and an injected decision failure returns AUDIT_UNAVAILABLE before an intentionally invalid ../ path can resolve; source regression forbids canonicalize/canonical_display_root/canonicalRoot/read_dir/WalkDir path-recursion patterns in read.rs; direct openat2 scan found all occurrences only in crates/workspace-io/src/openat.rs; production crates contain zero print!/println! call sites; WorkspaceManager has no node:fs fallback; packages/mcp-server still exposes neither workspace.* nor trust; public ws_ object does not expose private kc_ capability
Changed files reviewed: crates/protocol/src/lib.rs; crates/protocol/src/types.rs; crates/protocol/tests/protocol_contract.rs; crates/runtime/src/audit.rs; crates/runtime/src/dispatcher.rs; crates/workspace-io/src/lib.rs; crates/workspace-io/src/openat.rs; crates/workspace-io/src/read.rs; crates/workspace-io/src/registry.rs; packages/core/src/workspace-manager.test.ts; packages/core/src/workspace-manager.ts; packages/protocol/src/runtime-types.ts; schemas/runtime/request.schema.json; tests/fixtures/runtime/file.search.json; tests/fixtures/runtime/file.tree.json; tests/protocol/framing-parity.test.ts; tests/security/workspace-read-boundary.test.ts
Blueprint requirements covered: FD-relative read/tree/search authority after registration; 1 MiB inline read ceiling; parent/outside-symlink/nested-mount denial; directory-FD-only recursive traversal; recursive symlink directory non-descent; deterministic 2,000-entry ceiling; lexical UTF-8 search with binary skip, 200-match ceiling, and 256 KiB aggregate snippets; forbidden canonical-root pathname-recursion regression; audit decision before every file filesystem action; closed TS/Rust/JSON-Schema runtime contracts for file.tree/file.search; READY-only public WorkspaceManager facade that maps ws_ to private kc_ without exposing the capability
Known deviations: no architecture deviation. Search is intentionally lexical/case-sensitive and text-only because Task 9 does not specify regex/indexing semantics. Independent reviewer subagent remains unavailable through the current CodexPro action surface, so review used full unstaged + staged unified diff, complete read.rs inspection, fresh targeted/full verification, and explicit security scans without claiming independent reviewer coverage. Optional rustfmt remains unavailable on the host stable toolchain.
External prerequisites checked: Task 8 functional filesystem-boundary probe remains available on the host; linked worktree/branch/origin were already verified in this continuation; no Pranikah or CodexPro runtime dependency was added
Gate decision: Task 9 PASS; B1 NOT YET REVIEWED pending fresh combined Tasks 5–9 gate verification
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Gate B1 evidence record

```text
Gate: B1 — Trusted Workspace / Retained-FD Filesystem Boundary
Status: PASS
Reviewed implementation head: 2777b78 (Tasks 5–9 implementation + task evidence committed and pushed before gate review)
Fresh targeted verification: cargo test -p kodegpt-workspace-io openat PASS (4/4); cargo test -p kodegpt-workspace-io read PASS (8/8 selected by filter); cargo test -p kodegpt-workspace-io registry PASS (6/6); cargo test -p kodegpt-policy PASS (3/3); pnpm --filter @kodegpt/core test PASS (12/12); pnpm exec vitest run tests/security/workspace-trust.test.ts tests/security/audit-foundation.test.ts tests/security/workspace-read-boundary.test.ts PASS (9/9)
Fresh full verification: pnpm typecheck PASS across 12 TypeScript workspace projects; pnpm test PASS (58/58 across 13 files); cargo test --workspace PASS (3 policy, 8 protocol, 13 default-feature runtime, 26 workspace-io; all remaining crates/doc-tests green); cargo test -p kodegpt-runtime --features runtime-test-methods PASS (15/15)
Invariant 1 — persistent trusted identity: PASS via schema-v1 local trust store + fresh workspace-trust/security/full tests
Invariant 2 — same-path identity replacement detection: PASS via fresh workspace-trust replacement regression and workspace-io identity tests
Invariant 3 — bind alias/backing overlap rejection: PASS via fresh registry bind-alias/backing ancestor/device+inode overlap tests
Invariant 4 — retained root-FD authority: PASS via fresh registry revalidation test plus profile/file visible-path replacement regressions
Invariant 5 — mount topology fail closed: PASS via fresh registry unavailable/malformed topology regression
Invariant 6 — two-phase activation: PASS via fresh core/runtime lifecycle tests; READY is not published before valid activation acknowledgement
Invariant 7 — monotonic policy restriction: PASS via fresh Rust policy 3/3 plus runtime/core profile tests
Invariant 8 — public ws_ cannot operate while OPENING/CLOSING: PASS via fresh core lifecycle tests and registry phase enforcement
Invariant 9 — bounded close: PASS via fresh core close/deadline/retry tests
Invariant 10 — reusable exact openat2 boundary: PASS; fresh source scan found all four direct openat2 call sites only in crates/workspace-io/src/openat.rs and Task 8 tests confirm exact required semantics
Invariant 11 — no parent traversal: PASS via fresh openat/read boundary tests
Invariant 12 — no outside symlink escape: PASS via fresh openat/read/profile/tree tests
Invariant 13 — no magic-link escape: PASS via fresh /proc/self/fd regression
Invariant 14 — no nested-mount crossing: PASS via fresh NO_XDEV /proc crossing tests and functional probe
Invariant 15 — race-safe resolution: PASS via fresh symlink-swap race test; outside content is never opened
Invariant 16 — FD-relative read/tree/search only: PASS via fresh read tests and forbidden-pattern source regression; read.rs has no canonicalize/read_dir pathname recursion
Invariant 17 — recursive symlink directories not descended: PASS via fresh tree security test
Invariant 18 — audit decision before all file actions: PASS via fresh runtime audit fault regression and security source regression
Invariant 19 — no pathname authority after registration: PASS via fresh profile and read/tree/search visible-path replacement regressions; operations still use originally retained root FD
Invariant 20 — no trust via MCP: PASS; fresh packages/mcp-server scans found no workspace.* or trust surface
Invariant 21 — production stdout framed only: PASS; fresh crates scan found zero print!/println!; runtime stdout search found only the single writer in crates/runtime/src/main.rs, which serializes every response through write_frame before flush
Invariant 22 — no Pranikah/CodexPro runtime dependency: PASS; fresh regex scans found no Pranikah/CodexPro/.codexpro references in crates, packages, apps, root package.json, or root Cargo.toml
Additional fallback scan: packages/core/src/workspace-manager.ts has no node:fs import/fallback; the only node:fs hit in packages/core/src is test-only kernel-client.test.ts
Review discipline: gate started from clean committed worktree after Task 9 push; no per-task output was reused as the gate result; independent reviewer subagent is unavailable through the current CodexPro execution surface, so the gate used fresh targeted/full test execution, direct source scans, retained-FD/path-replacement regressions, and explicit 22-invariant checklist review without claiming independent reviewer coverage
Gate decision: PASS (B1)
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Task 10 evidence record

```text
Task: 10 — Connector Credential Lifecycle + Unified HTTP Trust
Status: DONE
Commit(s): 02d7a7c feat(auth): add connector verifier and unified http trust
RED command + expected failure: pnpm --filter @kodegpt/auth test → FAIL initially because credential-store.js, bearer-auth.js, and http-trust.js did not exist; pnpm --filter kodegpt test → FAIL initially because apps/cli/src/commands/auth.js did not exist. During GREEN/security review, the non-canonical authority regression initially failed because WHATWG URL normalization allowed 2130706433:43121 / 127.1:43121 to normalize to an allowed 127.0.0.1 authority; production authority validation was tightened rather than weakening the test. Targeted auth typecheck also exposed one test-only invalid Vitest generic matcher usage, which was corrected without changing production behavior.
GREEN command + result: pnpm --filter @kodegpt/auth test PASS (16/16: credential 4, bearer 3, HTTP trust 9); pnpm --filter kodegpt test PASS (5/5: workspace 2, auth CLI 3); pnpm --filter @kodegpt/auth typecheck PASS; pnpm --filter kodegpt typecheck PASS
Full task verification command + result: pnpm typecheck PASS across 12 TypeScript workspace projects; pnpm test PASS (77/77 across 17 files); cargo test --workspace PASS (3 policy, 8 protocol, 13 default-feature runtime, 26 workspace-io; remaining crates/doc-tests green); cargo test -p kodegpt-runtime --features runtime-test-methods PASS (15/15)
Security regression(s): first rotation creates a kgc_<16-char id>.<43-char base64url secret> credential where the secret is exactly 32 random bytes; durable connector.json stores only schemaVersion/id/verifier/createdAt/rotatedAt and tests prove neither plaintext token nor secret segment is persisted; auth directory is forced 0700 and verifier file 0600; writes use exclusive 0600 temp file + fsync + atomic rename + final file chmod + directory fsync; future schema versions and unexpected fields fail closed; verifier is independently proven as SHA-256("kodegpt-connector-v1\\0" + id + "\\0" + secret); canonical token length is rejected before base64 decode to avoid oversized malformed-input allocation; bearer format/id is validated before verification and matching verifier bytes use crypto.timingSafeEqual; authenticator reloads durable verifier on every request so the old token fails immediately after durable rotation without recreating the authenticator; CLI status exposes only configured/id/timestamps while rotate reveals only the newly issued plaintext value; HttpTrustConfig is the sole repository implementation source and combines allowed Host authorities, Origin hosts, optional public URL, and body ceiling; missing/bad Host, bad Origin, non-JSON media type, malformed/mismatched Content-Length, and oversized declared/actual bodies fail closed; non-canonical numeric Host/Origin aliases are rejected rather than normalized into the allowlist; @kodegpt/auth public exports are intentionally narrower than package internals
Changed files reviewed: apps/cli/package.json; apps/cli/src/commands/auth.test.ts; apps/cli/src/commands/auth.ts; packages/auth/package.json; packages/auth/src/bearer-auth.test.ts; packages/auth/src/bearer-auth.ts; packages/auth/src/credential-store.test.ts; packages/auth/src/credential-store.ts; packages/auth/src/http-trust.test.ts; packages/auth/src/http-trust.ts; packages/auth/src/index.ts; pnpm-lock.yaml
Blueprint requirements covered: kgc_ connector credential lifecycle with 32-byte random secret; plaintext returned only at rotation/initial creation; verifier-only restart state; exact domain-separated SHA-256 verifier; constant-time verifier comparison after format/id checks; immediate old-token invalidation after durable new verifier; CLI auth status/rotate surface; one HttpTrustConfig source; fail-closed Host/Origin/content-type/body-size guard; atomic schema-v1 durable state
Known deviations: no architecture deviation. The repository has no top-level CLI dispatcher yet, so auth.ts follows the existing command-module pattern used by workspace.ts instead of inventing a new CLI entrypoint in Task 10. Task 10 defines and verifies the pre-dispatch HTTP trust guard authority; actual transport ordering of HTTP trust → bearer auth → MCP dispatch necessarily depends on the Task 11 server and is not claimed as full-stack evidence yet. Independent reviewer subagent remains unavailable through the current CodexPro execution surface; review used full unstaged/staged unified diffs, direct source inspection, fresh targeted/full verification, and explicit security regression review.
External prerequisites checked: Node crypto provides randomBytes/createHash/timingSafeEqual; durable filesystem primitives used by the existing local state model are available on the current Linux host; pnpm workspace link for @kodegpt/auth was refreshed offline without changing remote dependencies; no Pranikah/CodexPro runtime dependency was added
Gate decision: Task 10 PASS; C1 NOT YET REVIEWED because Task 11 remains
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Task 11 evidence record

```text
Task: 11 — MCP 2026-07-28 Server, Exact Tool Annotations, Surface Contract
Status: DONE
Commit(s): 51ee91d feat(mcp): add strict annotated mcp 2026 surface
Dependency/API preflight: exact approved tuple installed without fallback — @modelcontextprotocol/server 2.0.0, @modelcontextprotocol/core 2.0.0, @modelcontextprotocol/node 2.0.0-beta.5, @modelcontextprotocol/client 2.0.0-beta.5 dev/test only. Runtime export probes confirmed McpServer, createMcpHandler, toNodeHandler, StdioServerTransport, serveStdio, and DiscoverRequest schema. A raw createMcpHandler server/discover probe with protocol metadata 2026-07-28 returned HTTP 200, supportedVersions=[2026-07-28], resultType=complete, and serverInfo meta; therefore the exact tuple passed the locked modern-wire preflight and no dependency amendment was required.
RED command + expected failure: pnpm --filter @kodegpt/mcp-server test → FAIL initially because surface-version/server modules did not exist; pnpm exec vitest run tests/integration/mcp-http.test.ts → FAIL initially because the HTTP transport/tool context did not exist; pnpm --filter @kodegpt/core test → FAIL after adding the public workspace-list regression because WorkspaceManager.listWorkspaces did not exist; pnpm --filter kodegpt test → FAIL after adding startup-order coverage because start.js did not exist. During GREEN, two test-contract corrections were made without weakening production policy: an explicit null sentinel was required to generate a discovery request with no _meta, and Mcp-Name mismatch was moved from unnamed server/discover to tools/call where the modern protocol applies the name header. Targeted typecheck also found a structural adapter/fake-generic mismatch, and the bounded HTTP body forwarder required an explicit ArrayBuffer BodyInit; these were corrected before final verification.
GREEN command + result: pnpm --filter @kodegpt/mcp-server typecheck PASS; pnpm --filter @kodegpt/mcp-server test PASS (3/3); pnpm exec vitest run tests/integration/mcp-http.test.ts tests/integration/mcp-stdio.test.ts PASS (7/7: HTTP 5, stdio 2); pnpm --filter kodegpt typecheck PASS; pnpm --filter kodegpt test PASS (9/9); pnpm --filter @kodegpt/core test PASS (13/13)
Full task verification command + result: pnpm typecheck PASS across 12 TypeScript workspace projects; pnpm test PASS (92/92 across 21 files); cargo test --workspace PASS (3 policy, 8 protocol, 13 default-feature runtime, 26 workspace-io; remaining crates/doc-tests green); cargo test -p kodegpt-runtime --features runtime-test-methods PASS (15/15)
Security/conformance regression(s): dependency-lock test pins the exact approved v2 tuple and rejects @modelcontextprotocol/sdk; one semantic registry feeds both HTTP and stdio; surface version is exactly 0.1; tools/list exposes exactly workspace.list/open/close/info, file.read/search/tree, profile.current/inspect, system.capabilities/health and no trust tool; read-only tools expose exactly readOnlyHint=true/destructiveHint=false/idempotentHint=true/openWorldHint=false while workspace.open/close use conservative non-read-only annotations; exact required input fields are snapshot-locked. Raw HTTP proves missing bearer 401, missing Host 400, invalid Host 421, invalid Origin 403, non-JSON 415, oversized body 413, missing _meta 400, missing clientCapabilities 400, protocol mismatch 400, Mcp-Method mismatch 400, tools/call Mcp-Name mismatch 400, valid/absent clientInfo discovery success, malformed clientInfo 400, complete discovery, exact serverInfo meta, complete tools/list, no Mcp-Session-Id, and GET/DELETE 405. HTTP request body is streamed with a configured byte ceiling before MCP parsing; accepted POSTs then pass the centralized HttpTrustConfig guard, bearer verification, and only then mcp.fetch. Raw modern stdio proves the same discovery/tool registry and exact annotations/schemas, while legacy initialize is rejected. WorkspaceManager.listWorkspaces publishes READY public ws_ snapshots only and never private kc_ capabilities. Startup regression locks state-root → audit → connector verifier → kernel → hello capability validation → trust/profile → managers → MCP → loopback bind, fails closed on unhealthy audit/unavailable filesystem boundary/runtime test methods, binds only 127.0.0.1, and reports secret-free status; shutdown is reverse-order. Production MCP source contains no session/SSE compatibility path, legacy initialize handler, workspace trust/untrust operation, or ext-apps server import. Production Rust stdout scan remains free of print!/println!, and no Pranikah/CodexPro runtime coupling was introduced.
Changed files reviewed: apps/cli/package.json; apps/cli/src/commands/start.test.ts; apps/cli/src/commands/start.ts; packages/core/package.json; packages/core/src/index.ts; packages/core/src/workspace-manager.test.ts; packages/core/src/workspace-manager.ts; packages/mcp-server/package.json; packages/mcp-server/src/annotations.ts; packages/mcp-server/src/dependency-lock.test.ts; packages/mcp-server/src/http.ts; packages/mcp-server/src/index.ts; packages/mcp-server/src/server.test.ts; packages/mcp-server/src/server.ts; packages/mcp-server/src/stdio.ts; packages/mcp-server/src/surface-version.ts; packages/mcp-server/src/tool-context.ts; packages/mcp-server/src/tools.ts; pnpm-lock.yaml; tests/integration/mcp-http.test.ts; tests/integration/mcp-stdio.test.ts
Blueprint requirements covered: exact MCP dependency tuple + modern compile/wire preflight; strict raw HTTP 2026-07-28 matrix; locked 11-tool semantic surface; centralized exact annotations; surface-version 0.1 name/required-field snapshot; one registry for HTTP/stdio; modern stdio with legacy rejection; exact tools/list annotations/schema verification; HTTP trust → bearer → MCP ordering; bounded pre-parse body reading; public-only workspace list; fail-closed kodegpt start command-module orchestration and safe status
Known deviations: no architecture deviation. The MCP server package uses the tuple's modern createMcpHandler/serveStdio entrypoints even though package-root exported legacy protocol constants still refer to older compatibility-era values; support was proven by actual 2026-07-28 raw-wire discovery rather than inferred from those constants. Task 11 wires the kodegpt start command module and production startup dependency chain; creation of a packaged executable/installer and packaged command smoke is intentionally Task 21. Independent reviewer subagent remains unavailable through the current CodexPro execution surface; review used full unstaged/staged unified diffs, direct authority-file inspection, exact dependency/API probes, fresh raw-wire integration tests, full repository verification, and explicit forbidden-surface scans without claiming independent reviewer coverage.
External prerequisites checked: exact MCP tuple resolved successfully in the workspace lockfile; modern HTTP/Node/stdio APIs and server/discover were proven locally before architecture implementation; Node HTTP/streams/WHATWG Request APIs required by the transport are available on Node 24.17.0; no Pranikah/CodexPro runtime dependency was added
Gate decision: Task 11 PASS; Gate C1 NOT YET REVIEWED until a fresh post-commit raw-wire gate run is completed
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Gate C1 evidence record

```text
Gate: C1 — Strict Modern MCP Raw-Wire / Auth Boundary
Status: PASS
Reviewed implementation head: c6c241e (Task 11 implementation 51ee91d + Task 11 evidence committed and pushed before gate review)
Fresh committed-state targeted verification: pnpm --filter @kodegpt/mcp-server test PASS (3/3); pnpm exec vitest run tests/integration/mcp-http.test.ts tests/integration/mcp-stdio.test.ts PASS (7/7); pnpm --filter kodegpt test PASS (9/9)
Fresh full verification: pnpm typecheck PASS across 12 TypeScript workspace projects; pnpm test PASS (92/92 across 22 files); cargo test --workspace PASS (3 policy, 8 protocol, 13 default-feature runtime, 26 workspace-io; remaining crates/doc-tests green); cargo test -p kodegpt-runtime --features runtime-test-methods PASS (15/15)
Exact dependency/surface proof: dependency-lock test freshly confirms @modelcontextprotocol/server=2.0.0, core=2.0.0, node=2.0.0-beta.5, client=2.0.0-beta.5 dev/test only and no @modelcontextprotocol/sdk dependency; surface test freshly confirms version 0.1 and the exact 11 semantic tool names/required fields
Modern HTTP proof: missing bearer 401; missing Host 400; invalid Host 421; invalid Origin 403; non-JSON 415; oversized body 413; missing _meta/clientCapabilities/protocol mismatch/method mismatch/name mismatch all fail; absent or valid clientInfo discovers successfully; malformed clientInfo fails; discovery is complete and advertises only 2026-07-28 with exact KodeGPT serverInfo; tools/list is complete and exact; no Mcp-Session-Id; GET/DELETE reject 405
Modern stdio proof: raw newline-delimited 2026-07-28 discovery and tools/list use the identical semantic registry, exact required schemas, and exact read-only/lifecycle annotations; legacy initialize receives an error and cannot negotiate a compatibility session
Authority/order proof: startup tests freshly prove state-root → audit → connector verifier → kernel → hello capability validation → trust/profile → managers → MCP → 127.0.0.1 bind, with fail-closed audit/filesystem gating and reverse-order shutdown; HTTP POST path remains bounded-body → centralized HttpTrustConfig → bearer verification → MCP dispatch; WorkspaceManager public listing excludes private kc_ capabilities
Forbidden-surface scans: packages/mcp-server/src contains no workspace.trust/workspace.untrust, Mcp-Session-Id/mcp-session-id, initialize handler, SSE/session compatibility path, or ext-apps server import. Search for @modelcontextprotocol/sdk in MCP source returns only the negative dependency-lock assertion. No Pranikah/CodexPro runtime coupling or Rust stdout print!/println! regression was introduced.
Review discipline: Gate C1 began from a clean worktree after Task 11 implementation/evidence push; all gate commands above were rerun post-commit rather than reusing pre-commit Task 11 output. Independent reviewer subagent remains unavailable through the current CodexPro execution surface; gate review used fresh raw-wire tests, full repository verification, exact dependency/surface snapshots, authority-order tests, and explicit forbidden-surface scans without claiming independent reviewer coverage.
Gate decision: PASS (C1)
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Task 12 execution evidence record

```text
Task: 12 — Hardened Bubblewrap Provider, Trusted Executable Identity, Root-FD Sandbox Binding
Status: DONE — mandatory functional sandbox acceptance is green on the current host; Task 13 may begin only after this evidence commit is pushed
Commit(s): `2f7f8b3` — feat(sandbox): bind root fd into hardened child namespace
RED command + expected failure: initial pnpm exec vitest run tests/security/sandbox-boundary.test.ts FAIL (3/3) before implementation because executable.rs, bubblewrap.rs, and execution.rs did not exist. Subsequent self-review REDs independently caught missing crate-wide spawn serialization, missing reusable trusted logical-executable resolver, externally constructible ENFORCED capability state, dot/path executable-name selection, an unpinned PathBuf child program, and caller-supplied `PWD`. Each local defect was made GREEN without weakening sandbox requirements. Before the host prerequisite fix, cargo test -p kodegpt-sandbox consistently failed only at the mandatory functional probe with SANDBOX_UNAVAILABLE: bwrap: setting up uid map: Permission denied, establishing the AppArmor/userns blocker rather than masking it.
GREEN/targeted evidence: cargo test -p kodegpt-sandbox PASS (7/7, including mandatory functional isolation); cargo test -p kodegpt-runtime execution PASS (1/1); cargo test -p kodegpt-runtime --features runtime-test-methods PASS (16/16); pnpm exec vitest run tests/security/sandbox-boundary.test.ts PASS (4/4); pnpm typecheck PASS; pnpm build PASS; pnpm test PASS (96/96 across 23 test files); targeted rustfmt --check for Task 12 Rust files PASS. Full-workspace cargo fmt --all -- --check still reports pre-existing formatting drift in unrelated files, so no unrelated mass-formatting was introduced.
Full task verification command + result: cargo test --workspace PASS with policy 3/3, protocol contract 8/8, runtime 14/14, sandbox 7/7, workspace-io 26/26, and zero test failures. Functional sandbox status is therefore probe-derived ENFORCED on this acceptance host, not inferred from version/metadata alone.
Security regression(s): fixed trusted executable directories only with root-owned/non-group-or-world-writable ancestor chain; generic logical executable names cannot select arbitrary paths; canonical target device/inode/mode/uid identity is pinned; both Bubblewrap and child executable identities are revalidated under a crate-wide process-spawn lock immediately before spawn; no arbitrary PATH resolution; duplicated retained READY root FD via --bind-fd/--ro-bind-fd; CLOEXEC inheritance window serialized; root-path rename/replacement probe; read-only FD-leak/write-bypass probe; private HOME/tmp/proc/dev; host HOME/other-workspace/state/PID/userns/NoNewPrivs/capability/env/network probes; ENFORCED state is probe-derived and not externally constructible; localhost/allowlist fail closed; no unsandboxed fallback; provider does not pre-create a process group that would conflict with Bubblewrap `--new-session`/`setsid`, and records the successful Bubblewrap PID as the session/process-group leader for cancellation. Source scan confirms production Command::new occurs only in the trusted version probe and actual Bubblewrap spawn; no Pranikah/CodexPro runtime coupling or workspace-path bind source is present.
Changed files reviewed: Cargo.lock; crates/sandbox/{Cargo.toml,src/lib.rs,src/executable.rs,src/bubblewrap.rs,src/capabilities.rs}; crates/workspace-io/src/registry.rs; crates/runtime/src/{main.rs,execution.rs}; tests/security/sandbox-boundary.test.ts; this tracker. Review was explicit self-review on the available CodexPro surface.
Blueprint requirements covered: Task 12.1–12.11 PASS with actual functional evidence; Task 12.12 implementation commit is `2f7f8b3`. C2 remains NOT YET REVIEWED because Task 13 and the combined gate review are still pending.
Known deviations: none in the product/security contract. The acceptance host requires a narrow site AppArmor prerequisite for trusted `/usr/local/bin/bwrap`; global unprivileged-userns restriction remains enabled.
External prerequisites checked: trusted `/usr/local/bin/bwrap` 0.11.2 remains root-owned regular mode 0755, non-setid, non-group/world-writable; kernel.unprivileged_userns_clone=1; user.max_user_namespaces=63195; kernel.apparmor_restrict_unprivileged_userns=1. `/etc/apparmor.d/kodegpt-bwrap-userns-restrict` is root:root 0644, compile-checks with apparmor_parser -Q -K, and was loaded with apparmor_parser -r. Root aa-status shows `kodegpt_bwrap` and `kodegpt_unpriv_bwrap` in enforce mode. Generic `unshare -Ur true` still fails at uid_map, proving the host exception is scoped to trusted Bubblewrap rather than disabling the global restriction.
Gate decision: NOT YET REVIEWED — Task 12 DONE; C2 awaits Task 13 plus fresh combined review
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Task 13 execution evidence record

```text
Task: 13 — Kernel Raw Output Spool Foundation
Status: DONE — implementation committed; C2 still requires a fresh combined gate review before Task 14
Commit(s): `700350c` — feat(artifacts): add bounded kernel output spool
RED command + expected failure: cargo test -p kodegpt-runtime spool FAIL before implementation with unresolved RAW_SPOOL_SOURCE_CAP_BYTES and RawSpoolStore imports from the new spool tests. The RED covered opaque ka_ identity/no host path, protected root/file permissions, exact >64 MiB truncation, audit success/failure without output bytes, and closed runtime artifact schema.
GREEN command + result: cargo test -p kodegpt-runtime spool PASS (5/5 after self-review added an audit-decision fail-closed regression). The writer accepts multiple source chunks into one spool, stops exactly at 67,108,864 bytes, sets sourceTruncated only when additional source bytes are observed, and rejects a second spool for the same private ex_ execution so the cap cannot be bypassed by reopening.
Full task verification command + result: cargo test -p kodegpt-runtime PASS (19/19); cargo test -p kodegpt-runtime --features runtime-test-methods PASS (21/21); pnpm exec vitest run tests/security/audit-foundation.test.ts PASS (6/6); pnpm typecheck PASS; pnpm build PASS; pnpm test PASS (96/96 across 23 files); cargo test --workspace PASS including sandbox functional probes 7/7. rustfmt --edition 2024 --check crates/runtime/src/spool.rs PASS.
Security regression(s): serialized RawSpoolMetadata has only schemaVersion/artifactId/mediaType/bytesWritten/sourceTruncated and never a raw/host path; ka_ IDs contain no slash/traversal; artifacts/raw is forced 0700 and spool files 0600/create_new; execution IDs are validated and one spool is issued per execution; audit decision persists before artifact creation and injected decision failure leaves the raw directory empty; creation failure receives a failed outcome; audit records never receive source/output bytes. Audit schema was also brought into alignment with the already-existing file_read/file_tree/file_search actions while adding artifact_spool_create.
Changed files reviewed: crates/runtime/src/{spool.rs,audit.rs,main.rs}; schemas/runtime/{artifact.schema.json,audit.schema.json}. A failed rustfmt invocation briefly reformatted unrelated audit.rs blocks; audit.rs was restored to HEAD and only the two intended Task 13 action additions were reapplied before final verification.
Blueprint requirements covered: 13.1 opaque ID/no host path; 13.2 protected state-root artifacts/raw; 13.3 exact 64 MiB combined per-execution source cap plus bytes/media/truncation metadata; 13.4 deterministic >64 MiB truncation; 13.5 audited creation/failure without output bytes; 13.6 verification PASS. Gate C2 is intentionally not claimed from pre-commit evidence.
Known deviations: none in the Task 13 product/security contract. Public artifact:// mapping, bounded reads, TTL/quota, symlink-safe retention, and active-writer deletion protection remain Task 17 by plan.
External prerequisites checked: Task 12 trusted Bubblewrap/AppArmor functional acceptance remains green inside cargo test --workspace; no new host prerequisite was introduced by Task 13.
Gate decision: NOT YET REVIEWED — C2 must be rerun fresh after Task 13 evidence is committed/pushed
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Gate C2 review record

```text
Gate: C2 — Combined connector auth, MCP surface, sandbox, execution identity, and raw-spool review
Decision: PASS (C2) — fresh review performed from clean pushed commit ab709d2; Task 14 may begin only after this gate evidence commit is pushed
Fresh verification: pnpm --filter @kodegpt/auth test PASS (16/16); pnpm --filter @kodegpt/mcp-server test PASS (3/3); pnpm exec vitest run tests/integration/mcp-http.test.ts PASS (5/5); pnpm exec vitest run tests/integration/mcp-stdio.test.ts PASS (2/2); cargo test -p kodegpt-sandbox PASS (7/7 including mandatory functional isolation); cargo test -p kodegpt-runtime execution PASS (1/1); cargo test -p kodegpt-runtime spool PASS (5/5); pnpm exec vitest run tests/security/sandbox-boundary.test.ts PASS (4/4); pnpm typecheck PASS; pnpm build PASS; pnpm test PASS (96/96 across 23 files); cargo test --workspace PASS with policy 3/3, protocol contract 8/8, runtime 19/19, sandbox 7/7, workspace-io 26/26, and no failures.
Connector-auth review: credential store persists only schemaVersion/id/verifier/timestamps, generates a 12-byte id plus 32-byte secret, derives the domain-separated SHA-256 verifier over kodegpt-connector-v1\0<id>\0<secret>, durably writes verifier-only state at protected permissions, and bearer auth validates canonical token format/id before a length-checked timingSafeEqual verifier comparison. Rotation remains verifier replacement with the old token invalid immediately after the durable new store is active.
HTTP/MCP review: one HttpTrustConfig owns allowed Host/Origin/public URL/body ceiling; request handling reads only to the configured body ceiling, enforces canonical Host then Origin then JSON/content-length/body-size policy, checks bearer authentication, and only then dispatches MCP. The server uses @modelcontextprotocol/server v2 with legacy:"reject", one semantic 11-tool registry, MCP surface version exactly 0.1, exact centralized read-only/lifecycle annotations, and no trust-establishment tool or legacy session compatibility path.
Sandbox/execution review: functional acceptance is actual probe-derived evidence, not version-only; trusted executable logical names resolve only from fixed trusted system directories with canonical device/inode/mode/uid identity and immediate revalidation; production Rust Command::new occurs only in the trusted version probe and actual Bubblewrap launch; root workspace binding uses only duplicated retained READY root FD via --ro-bind-fd/--bind-fd; the root-path drift probe, HOME/other-workspace/state/PID/userns/NoNewPrivs/capability/env/network probes pass; no unsandboxed fallback, --not-a-security-boundary, or parent .process_group(0) exists in production.
Raw-spool review: one private ka_ spool is issued per private ex_ execution, protected artifacts/raw and file permissions are enforced, the source stream stops exactly at 67,108,864 bytes with deterministic sourceTruncated metadata, audit decision precedes artifact creation and failures are audited without output bytes, and serializable metadata/schema contains no raw/host spool path. Public artifact facade/retention remain intentionally deferred to Task 17.
Forbidden/degradation scans: Mcp-Session-Id occurs only in negative test/docs; @modelcontextprotocol/sdk occurs only in a negative dependency-lock assertion; @modelcontextprotocol/ext-apps/server has no package-source hit; sandbox source has no fallback; --not-a-security-boundary and .process_group(0) occur only in negative assertions/docs; schemas/runtime contains no hostPath/rawPath; production Rust process spawn remains exactly two controlled locations. No Pranikah/CodexPro runtime coupling was introduced.
Known deviations: none requiring amendment. The narrow host AppArmor profile for trusted /usr/local/bin/bwrap remains an acceptance-host prerequisite while the global unprivileged-userns restriction stays enabled; Task 22 must re-prove host security acceptance later.
Gate decision: PASS (C2)
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Task 14 execution evidence record

```text
Task: 14 — Race-Safe File Write/Edit and Self-Protection
Status: DONE — implementation `31ee853`, follow-up mutation integration hardening `30727e9`, and fresh full verification are durable on the feature branch; Task 15 may begin after this tracker correction is pushed
Commit(s): `31ee853` — feat(files): add race-safe protected mutations; `30727e9` — test(files): harden mutation integration coverage
RED command + expected failure: pnpm exec vitest run tests/security/self-protection.test.ts initially FAIL (2/3) because the public MCP surface had not yet registered file.write/file.edit. During integration, pnpm typecheck also exposed a stale CLI WorkspaceManagerToolAdapter test fixture missing writeFile/editFile; the fixture was synchronized without changing runtime behavior.
GREEN/targeted evidence: cargo test -p kodegpt-workspace-io write PASS (4/4 including traversal/outside-symlink denial, exact-count edit conflict, and symlink-swap race); cargo test -p kodegpt-runtime file_mutations PASS (1/1); pnpm exec vitest run tests/security/self-protection.test.ts PASS (3/3); pnpm --filter @kodegpt/mcp-server test PASS (3/3); pnpm --filter @kodegpt/core test PASS (13/13); pnpm test:protocol PASS (10/10); cargo test -p kodegpt-protocol PASS (8/8 contract tests).
Full task verification command + result: pnpm typecheck PASS across all 12 TypeScript projects; pnpm build PASS; pnpm test PASS (99/99 across 24 files); cargo test --workspace PASS with policy 3/3, protocol contract 8/8, runtime 20/20, sandbox 7/7 including mandatory functional isolation, workspace-io 30/30, and no failures.
Security regression(s): mutations remain retained-root-FD relative; parent traversal/outside symlink/cross-workspace/KodeGPT-state writes fail closed; same-parent atomic replacement uses random O_EXCL|O_NOFOLLOW temporary file, file fsync, destination revalidation, renameat, and parent fsync; exact text edit writes nothing on replacement-count conflict; observe and allowWrite=false policies deny mutation; durable audit decision precedes mutation and injected audit failure leaves no file; mutation contents are absent from audit; public MCP accepts only opaque workspaceId and exposes conservative readOnly=false/destructive=true/idempotent=false/openWorld=false annotations.
Changed files reviewed: Cargo.lock; apps/cli/src/commands/start.test.ts; crates/protocol/src/{lib.rs,types.rs}; crates/runtime/src/{audit.rs,dispatcher.rs}; crates/workspace-io/{Cargo.toml,src/lib.rs,src/registry.rs,src/write.rs}; packages/core/src/{workspace-manager.ts,workspace-manager.test.ts}; packages/mcp-server/src/{annotations.ts,index.ts,server.test.ts,tool-context.ts,tools.ts}; packages/protocol/src/runtime-types.ts; schemas/runtime/{audit.schema.json,request.schema.json}; tests/fixtures/runtime/{file.write.json,file.edit.json}; tests/integration/{mcp-http.test.ts,mcp-stdio.test.ts}; tests/protocol/framing-parity.test.ts; tests/security/self-protection.test.ts.
Blueprint requirements covered: 14.1 security matrix PASS; 14.2 FD-relative atomic write PASS; 14.3 exact-count edit conflict/pass; 14.4 conservative mutating MCP annotations; 14.5 durable audit decision/outcome with fail-closed regression; 14.6 required verification plus full repository verification PASS; 14.7 implementation commit 31ee853.
Known deviations: none in the Task 14 product/security contract.
External prerequisites checked: Task 12 Bubblewrap/AppArmor functional acceptance remained green inside cargo test --workspace.
Gate decision: Task 14 DONE; Batch D remains in progress and Gate D1 is not reviewed until Tasks 15–18 complete.
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Task 15 execution evidence record

```text
Task: 15 — Read-Only Sandboxed Git Inspection
Status: DONE — implementation committed as 0822f78; Task 16 may begin after this evidence is durably committed
Commit(s): `0822f78` — feat(git): add read-only sandboxed git inspection
RED command + expected failure: pnpm exec vitest run tests/security/git-helper-isolation.test.ts initially FAIL (3/3) because git.rs, Git audit actions, and MCP git.status/git.diff did not exist. During GREEN, cargo test -p kodegpt-runtime git exposed a second security RED: a repository-defined clean filter executed during status and returned HELPER_EXECUTED/exit 128; the implementation was hardened to discover local filter driver names inside the same read-only Bubblewrap boundary and neutralize clean/smudge/process/required configuration for both status and diff.
GREEN/targeted evidence: cargo test -p kodegpt-runtime git PASS (1/1 malicious fsmonitor/external-diff/textconv/filter fixture with repository fingerprint unchanged); pnpm exec vitest run tests/security/git-helper-isolation.test.ts PASS (3/3); pnpm test:protocol PASS (10/10); cargo test -p kodegpt-protocol PASS (8/8 contract tests); pnpm --filter @kodegpt/mcp-server test PASS (3/3); pnpm --filter @kodegpt/core test PASS (13/13); pnpm typecheck PASS.
Full task verification command + result: pnpm build PASS; pnpm test PASS (102/102 across 25 files); cargo test --workspace PASS with policy 3/3, protocol contract 8/8, runtime 21/21, sandbox 7/7 including functional isolation, workspace-io 30/30, and no failures.
Security regression(s): git.status/git.diff always launch trusted revalidated git only through Bubblewrap using the duplicated READY retained root FD mounted read-only; network is denied; environment is explicit/clean with GIT_OPTIONAL_LOCKS=0, system/global config disabled, pager/prompt disabled, and LC_ALL fixed; core.fsmonitor, hooksPath, credential helper, diff.external, textconv, submodule traversal, and arbitrary local filter drivers are neutralized; output is drained to bounded raw spool before preview truncation; private ex_/ka_ identities and process-group/PID are not serialized to public MCP; durable Git audit decision precedes root duplication/spawn and outcome is recorded after completion; malicious helper fixture proves no helper output and byte-for-byte repository fingerprint stability.
Changed files reviewed: Cargo.lock; apps/cli/src/commands/start.test.ts; crates/protocol/src/{lib.rs,types.rs}; crates/runtime/{Cargo.toml,src/audit.rs,src/dispatcher.rs,src/git.rs,src/main.rs}; packages/core/src/{workspace-manager.ts,workspace-manager.test.ts}; packages/mcp-server/src/{server.test.ts,tool-context.ts,tools.ts}; packages/protocol/src/runtime-types.ts; schemas/runtime/{audit.schema.json,request.schema.json}; tests/fixtures/runtime/{git.status.json,git.diff.json}; tests/integration/{mcp-http.test.ts,mcp-stdio.test.ts}; tests/protocol/framing-parity.test.ts; tests/security/git-helper-isolation.test.ts.
Blueprint requirements covered: 15.1 malicious Git helper security matrix PASS; 15.2 retained-FD RO mount enforced; 15.3 helper-capable environment/config hardened; 15.4 private execution registration plus raw-spool-before-preview; 15.5 durable Git audit decision/outcome; 15.6 required verification plus full repository regression PASS; 15.7 implementation commit 0822f78.
Known deviations: repository-wide `cargo fmt --check` remains a pre-existing formatting-baseline issue across many already-committed Rust files from earlier tasks; no Task 15 correctness/security verification is failing. Resolve globally only at the final quality gate to avoid unrelated formatting churn during security task execution.
External prerequisites checked: trusted /usr/local/bin/bwrap >=0.11.2 and the Task 12 functional Bubblewrap/AppArmor probes remain green inside cargo test --workspace.
Gate decision: Task 15 DONE; Batch D remains in progress and Gate D1 is not reviewed until Tasks 16–18 complete.
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Task 16 execution evidence record

```text
Task: 16 — Sandboxed Process Run/Status/Cancel
Status: DONE
Commit(s): `17b6cf5` — chore: complete kodegpt v0.1 release-candidate gates
RED/GREEN evidence: process method/schema/parity and policy coverage were synchronized during the implementation cycle; targeted `cargo test -p kodegpt-runtime process` reached PASS (5/5) and `tests/security/process-policy.test.ts` is green.
Fresh final verification: pnpm test PASS (151/151); pnpm test:rust PASS with runtime 29/29 and sandbox 7/7; pnpm test:protocol PASS (10/10); pnpm test:security PASS (34/34); pnpm test:acceptance PASS (3/3).
Security regression(s): structured logical executable + argv only; no shell-string public primitive; effective process/write policy enforced; cwd retained-root bounded; env allowlist enforced; child executes through trusted Bubblewrap boundary; public op_ identity contains no PID/PGID/private ex_; workspace close/cancel terminates process groups; registry-poison path kills a spawned child before fail-closed return.
Changed files reviewed: crates/runtime/src/process.rs plus dispatcher/audit/protocol/schema/core/MCP integration and process fixtures/tests.
Blueprint requirements covered: Task 16 process primitive, status/cancel, opaque public identity, cancellation tree, sandbox/policy boundary, D1 functional behavior.
Known deviations: none
External prerequisites checked: Bubblewrap 0.11.2 functional probes pass on the current acceptance host.
Gate decision: PASS (Task 16 DONE, durably committed in 17b6cf5).
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Task 17 execution evidence record

```text
Task: 17 — Artifact Facade, Bounded Read, Retention
Status: DONE
Commit(s): `17b6cf5` — chore: complete kodegpt v0.1 release-candidate gates
Targeted evidence: cargo test -p kodegpt-runtime artifact PASS (3/3); @kodegpt/artifacts tests PASS (3/3); MCP/core integration remains green in the final suite.
Fresh final verification: pnpm test PASS (151/151); pnpm test:security PASS (34/34); pnpm test:integration PASS (20/20); pnpm test:acceptance PASS (3/3).
Security regression(s): only artifact://ka_... is public; host spool paths/private execution IDs are absent; per-read ceiling <=1 MiB; canonical Base64 validation; symlink rejection; 24h completed-artifact TTL; 1 GiB aggregate reservation; 64 MiB single-source cap; active writers are never retention victims.
Changed files reviewed: packages/artifacts/src/*; crates/runtime/src/spool.rs and artifact RPC/schema/MCP wiring.
Blueprint requirements covered: Task 17.1–17.6 local behavior PASS.
Known deviations: none
External prerequisites checked: none beyond accepted local state filesystem/runtime.
Gate decision: PASS (Task 17 DONE, durably committed in 17b6cf5).
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Task 18 execution evidence record

```text
Task: 18 — Audit Diagnostics and End-to-End Correlation
Status: DONE
Commit(s): `17b6cf5` — chore: complete kodegpt v0.1 release-candidate gates
Targeted evidence: cargo test -p kodegpt-runtime audit PASS (12/12); @kodegpt/audit tests PASS (2/2); audit-redaction acceptance PASS (1/1).
Fresh final verification: pnpm test PASS (151/151); pnpm test:security PASS (34/34); pnpm test:acceptance PASS (3/3); Rust workspace PASS.
Security regression(s): diagnostics read only the fixed <stateRoot>/logs/security/audit.jsonl path; caller limit clamps 1..200; public correlation keeps req_/op_ and strips kc_/ex_; connector token/env/argv secret markers have zero plaintext audit occurrences; audit failures remain fail closed.
Changed files reviewed: packages/audit/src/*; runtime audit/correlation integration; tests/security/audit-redaction.test.ts.
Blueprint requirements covered: Task 18.1–18.5 local behavior PASS and D1 security evidence.
Known deviations: none
External prerequisites checked: protected local audit state only.
Gate decision: PASS (Task 18 DONE, durably committed in 17b6cf5).
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Tasks 19–20 Batch E evidence record

```text
Tasks: 19 — MCP Apps Dev Console; 20 — Declarative Extensions and HTTP/zrok Exposure
Status: DONE for the zrok replacement deterministic gate: fresh full local verification PASS and GitHub CI run `31444498778` on `cf43bb6` PASS; real ChatGPT host observation is the remaining product-evidence gate.
Commit(s): `17b6cf5` — original Batch E baseline; `dae0526` — historical managed-ngrok amendment; `beabaf1` + `bedf804` — cold-run Vitest/Cargo determinism fixes; `03b9de3` + `9f657a3` — approved zrok design/plan; `3e9938d` + `1008fd5` — zrok exposure core and production CLI replacement; `a0aa8cc` — zrok metadata query implemented with structured `spawn` rather than Node `exec/execFile`, preserving the Rust user-process fallback guard.
Targeted zrok evidence: expose-zrok unit PASS (12/12); packaged CLI expose integration with fake `zrok2` PASS (1/1); manual-exposure ownership PASS (3/3); focused zrok/start/HTTP query-auth regression PASS (27/27). RED/GREEN coverage proves reserved-name parsing/resolution, exact loopback zrok argv, structured readiness, first-run credential issuance only after readiness, existing-verifier reuse, sanitized child failures, startup/readiness/credential cleanup, and idempotent close. `kodegpt expose ngrok` is rejected and ngrok production code/tests have been removed.
Fresh exact zrok-candidate local verification: `pnpm build` PASS; `pnpm typecheck` PASS; `pnpm test` PASS (177/177 across 49 files); `cargo fmt --all -- --check` PASS; sandbox PASS (7/7); Rust workspace PASS (policy 3/3, protocol contract 8/8, runtime 29/29, sandbox 7/7, workspace-io 30/30); protocol PASS (10/10); integration PASS (28/28 across 11 files); security PASS (34/34); isolation PASS (3/3); acceptance PASS (3/3); forbidden scan PASS; package smoke PASS. Package-smoke checksums: CLI tarball `c89473c199f37961991bf2fbc090818a51af7c3944858de287443e23dde85b47`; runtime package `052a3034c307c1ca63dd5659d24023b50181615f45e9bb308b41e37cdf3a263d`; runtime binary `c973814aa450790e63efaaa752c47a1ca62f918767022a14e11478c74442050f`.
Cold-run proof: after deleting ignored `target/` and the staged runtime binary, the canonical serialized `pnpm test` passed 177/177 across 49 files in ~84 s. This proves the zrok replacement preserves the cold Cargo/Vitest determinism fix established after GitHub runs `31381996884` and `31384012828`. GitHub CI run `31444498778` on `cf43bb6` then passed the complete deterministic v0.1 gate sequence, including TypeScript, Bubblewrap, Rust, protocol, integration, security, isolation, acceptance, forbidden-pattern, and clean-install package-smoke gates.
Security/conformance regression(s): normal `kodegpt start` remains loopback-only, requires an existing connector verifier, and owns no tunnel subprocess; `kodegpt bridge` remains tunnel-independent; only explicit `kodegpt expose zrok` may bootstrap a temporarily missing verifier while query compatibility + public URL are active. zrok is forced into locally supervised proxy mode and receives only a `127.0.0.1` upstream. Reserved-name/readiness metadata is consumed structurally, raw zrok status JSON is never logged, the credential-bearing query field is removed before downstream MCP dispatch, and KodeGPT does not manage zrok account/environment credentials.
Changed files reviewed: existing Batch E/auth/start infrastructure plus `apps/cli/src/commands/expose-zrok.ts`, `apps/cli/src/main.ts`, zrok unit/executable integration tests, manual exposure invariants, compatibility docs, and historical supersession markers.
Blueprint requirements covered: Tasks 19.1–19.14 and amended 20.1–20.8 local behavior PASS under the zrok replacement design.
Known deviations: no generic tunnel-provider framework by design; v0.1 implements zrok only. zrok namespace/name provisioning and zrok Agent ownership remain outside KodeGPT.
External prerequisites checked: deterministic tests use a fake `zrok2` executable and require no network. The real machine has zrok v2 enabled and a reserved `public:kodegpt-dev` name; real-host observation still remains external.
Gate decision: ZROK REPLACEMENT DETERMINISTIC + CI PASS; real ChatGPT host observation through `zrok-public-https-query-credential` is the remaining product-evidence gate.
Reviewed at: 2026-08-11 Asia/Jakarta
```

### Task 21 execution evidence record

```text
Task: 21 — Linux Packaging and Clean-Install Acceptance
Status: DONE
Commit(s): `17b6cf5` — chore: complete kodegpt v0.1 release-candidate gates
RED/GREEN evidence: clean-install smoke exposed and corrected four independent packaging defects: clean PATH hid npm; duplicate shebang; runtime executable mode lost after npm install; obsolete MCP per-request metadata keys. Each failure advanced to the next boundary only after its root cause was corrected.
Fresh verification: pnpm verify:package PASS. Release runtime excludes test RPC behavior; runtime package installs executable; packed CLI has no unresolved workspace:*; staged and packed runtime SHA-256 match; clean temporary HOME/prefix doctor/start/discover/health/shutdown smoke green.
Latest final3 checksum evidence: CLI tarball 16a320d0bb9c7326fe9a4c66be92c377a50ad601bf9f35f1b5852847a7b6ba7f; runtime package 052a3034c307c1ca63dd5659d24023b50181615f45e9bb308b41e37cdf3a263d; runtime binary c973814aa450790e63efaaa752c47a1ca62f918767022a14e11478c74442050f.
Security regression(s): production resolver uses package-owned runtime, explicit override only in development/test, no arbitrary PATH/Pranikah/CodexPro runtime search; clean install does not rely on repository node_modules PATH.
Changed files reviewed: apps/cli runtime resolver/main/build script/package; packages/runtime-linux-x64/package.json; scripts/stage-runtime.mjs; scripts/package-smoke.mjs; packaged-runtime integration test; root package script.
Blueprint requirements covered: 21.1–21.10 local E1 behavior PASS.
Known deviations: none
External prerequisites checked: Linux x64/Node/npm/pnpm/Rust toolchain available.
Gate decision: PASS (Task 21 DONE, durably committed in 17b6cf5).
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Task 22 execution evidence record

```text
Task: 22 — Full Kernel/Workspace/Sandbox Security Acceptance
Status: DONE
Commit(s): `17b6cf5` — chore: complete kodegpt v0.1 release-candidate gates
Fresh required verification: pnpm test:security PASS (34/34 across 11 files); pnpm test:rust PASS with policy 3/3, protocol contract 8/8, runtime 29/29, sandbox 7/7, workspace-io 30/30; no skipped security invariant.
Acceptance additions: tests/security/security-invariants.test.ts proves new file.read/process.run requests fail with RUNTIME_UNAVAILABLE after kernel death and workspace/process facades contain no TypeScript filesystem or user-process fallback.
Security regression(s): trust identity/overlap/lifecycle, openat2/no-xdev/races, self-protection, Bubblewrap HOME/PID/userns/NoNewPrivs/capabilities/network, policy writeability, executable/Git helper isolation, audit/artifact limits/redaction, and kernel-death fail-closed behavior remain green.
Changed files reviewed: security acceptance tests plus all security-sensitive Task 16–21 source paths through the final architecture scan.
Blueprint requirements covered: Task 22.1–22.10 local E2 security half PASS.
Known deviations: none
External prerequisites checked: mandatory Bubblewrap functional probes executed and PASS; no skip-on-missing-host behavior used.
Gate decision: PASS (Task 22 DONE, durably committed in 17b6cf5).
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Task 23 execution evidence record

```text
Task: 23 — MCP 2026 + Tool Surface + MCP Apps Conformance Acceptance
Status: DONE
Commit(s): `17b6cf5` — chore: complete kodegpt v0.1 release-candidate gates
RED/GREEN evidence: integration RED identified HTTP/stdio surface snapshots missing artifact.read, console.state, and extension.list; snapshots were synchronized before full-stack/conformance acceptance was added.
Fresh required verification: pnpm test:integration PASS (20/20 across 8 files); pnpm test:acceptance PASS (3/3); full pnpm test PASS (151/151).
Conformance regression(s): two trusted workspace lifecycle; write/read/search/tree; read-only Git; foreground/background process/status/cancel; artifact read; console state; extension list; clean close; exact protocol 2026-07-28; semantic surface 0.1; 21-tool names/required inputs; locked annotations; no session/legacy trust/shell path; exact Apps MIME/CSP/nested metadata; per-request Apps capability; non-UI text/structured fallback.
Changed files reviewed: tests/integration/full-stack.test.ts; mcp-conformance.test.ts; mcp-http/stdio snapshots; MCP Apps tests and current server surface.
Blueprint requirements covered: Task 23.1–23.8 local E2 protocol/apps half PASS.
Known deviations: none
External prerequisites checked: none beyond the local MCP/runtime acceptance host.
Gate decision: PASS (Task 23 DONE, durably committed in 17b6cf5).
Reviewed at: 2026-08-10 Asia/Jakarta
```

### Task 24 execution evidence record

```text
Task: 24 — ChatGPT Host Compatibility Claim Gate and Pranikah Isolation Gate
Status: DONE
Commit(s): `17b6cf5` — original claim-gate baseline; `03b9de3` + `9f657a3` — approved zrok replacement contract; `3e9938d` + `1008fd5` + `a0aa8cc` — zrok core/production CLI implementation and scanner-safe metadata-query fix; `cf43bb6` — full local verification evidence; `1199de6` — exact host candidate with GitHub CI run `31444942546` PASS.
Host evidence: the actual ChatGPT web host discovered and invoked KodeGPT through `zrok-public-https-query-credential`. Observed actions include workspace listing/opening, file read, file edit/write with filesystem verification, and foreground `process.run` using Python with exit code 0. No separate write-confirmation prompt was captured; the write was explicitly requested by the user and executed successfully. MCP Apps rendering itself was not observed in this host run; normal text/tool fallback remained usable.
Isolation evidence: the first two guard windows were invalidated by an independently running VS Code Codex process in `/home/sauron/dev/Pranikah-Labs` (`just verify` / `just e2e` / Docker Compose), which changed Pranikah tracked/generated files and listeners during those windows. KodeGPT trust remained limited to the KodeGPT repository and `/tmp/kodegpt-host-workspace`. After that independent Pranikah activity finished and a stability pair returned `guard unchanged`, a final public-zrok acceptance (`workspace.open` → file write/read → `process.run` → close) was enclosed by fresh Pranikah snapshots and the compare returned `guard unchanged`.
Claim discipline: `CHATGPT_HOST_OBSERVED` and `WRITE_OBSERVED` are supported for the exact tested host/connection path. The later 2026-08-12 follow-up also observed `skill.list` / `skill.inspect` / `skill.load` action exposure reaching KodeGPT. Apps rendering and a positive non-empty configured-catalog skill round trip remain explicitly NOT OBSERVED and must not be inferred from deterministic tests or action registration alone.
Changed files reviewed: docs/compatibility/chatgpt.md; bridge/start/expose CLI paths; HTTP compatibility layer; fake-zrok integration; tests/host/README.md; scripts/host-compatibility-checklist.mjs; host checklist tests.
Blueprint requirements covered: Task 24 generic docs, actual host observation, write observation, text fallback observation, and final Pranikah isolation obligation PASS.
Known deviations: MCP Apps UI rendering was not observed in the real host session; semantic tool/text fallback worked and the release claim remains scoped accordingly.
External prerequisites checked: real machine zrok v2 environment was enabled with reserved `public:kodegpt-dev`; machine-specific zrok/account data, connector credentials, host evidence JSON, and Pranikah guard manifests remain outside Git.
Gate decision: PASS for the observed core host categories — `CHATGPT_HOST_OBSERVED` + `WRITE_OBSERVED` + final Pranikah isolation compare `guard unchanged`; Apps rendering and positive configured-catalog skill round trip remain granular pending observations rather than reopening the whole host gate.
Reviewed at: 2026-08-11 Asia/Jakarta
```

### Task 25 execution evidence record

```text
Task: 25 — CI, Performance Baseline, Forbidden Patterns, Final Verification
Status: DONE
Commit(s): `17b6cf5` — chore: complete kodegpt v0.1 release-candidate gates
CI/guard implementation: .github/workflows/ci.yml provisions Node 24, pnpm 10.15.0, Rust stable, exact upstream Bubblewrap 0.11.2 commit 1b80120ef26a28e065e67f89bfef873f13bdd317 with support_setuid=false/root:root 0755, then runs frozen install, rustfmt check, deterministic TS/Rust/protocol/integration/security/isolation/acceptance/forbidden/package gates. ci-contract tests lock the ordered sequence and are green.
Fresh final3 guarded command set after repository-wide Rust formatting: pnpm install --frozen-lockfile --force PASS; `cargo fmt --all -- --check` PASS; typecheck PASS; build PASS; pnpm test PASS (151/151 across 44 files); cargo test -p kodegpt-sandbox PASS (7/7); pnpm test:rust PASS with runtime 29/29; protocol 10/10; integration 20/20; security 34/34; isolation 3/3; acceptance 3/3; forbidden scan PASS; package smoke PASS; performance baseline PASS; final3 Pranikah compare `guard unchanged`.
Post-final3 CI/docs delta verification after adding rustfmt to CI/release checklist: `cargo fmt --all -- --check` PASS; pnpm test PASS (151/151); pnpm test:integration PASS (20/20, including ci-contract 2/2); pnpm verify:forbidden PASS; `/tmp/kodegpt-pranikah-postci-before.json` vs `...-after.json` compare `guard unchanged`.
Final zrok/host closure verification on 2026-08-11: `pnpm build` PASS; `pnpm typecheck` PASS; `pnpm test` PASS (177/177 across 49 files); `pnpm verify:forbidden` PASS; package smoke PASS; `cargo fmt --all -- --check` PASS; `cargo test --workspace` PASS. Exact zrok candidate `1199de6` passed GitHub CI run `31444942546`; real ChatGPT host discovery/read/write/edit/process evidence was captured; final stable Pranikah isolation acceptance returned `guard unchanged`.
Performance contract: 5 warmups + 30 measured iterations, record-only/no hard threshold. Final3 median/p95 ms: hello IPC 0.514/0.662; console.state 9.949/20.892; workspace.open 33.237/39.226; workspace.info 9.182/28.631; small read 12.398/29.505; small tree 12.063/22.014; git.status 66.772/71.360; trivial process 48.735/54.068.
Architecture review: product apps/packages/crates have no Pranikah/CodexPro runtime coupling; workspace/execution facades have no TS filesystem/process fallback; MCP production surface has no kc_/ex_/capabilityId, legacy session/trust/shell/deprecated Apps path; security-sensitive workspace I/O has no canonicalize-then-open; Git helper/config restrictions remain explicit; workspace Bubblewrap authority remains retained-FD via --ro-bind-fd/--bind-fd; pnpm verify:forbidden passes.
Known deviations: none
Changed files reviewed: .github/workflows/ci.yml; tests/performance/*; scripts/forbidden-patterns.mjs; docs/release/v0.1-checklist.md; Task 16–24 accumulated worktree diff and architecture scans.
Blueprint requirements covered: Task 25.1–25.7 local behavioral/quality evidence; 25.8 durability commit satisfied; 25.9 host/tag prerequisites are now satisfied subject only to final exact-release commit CI and branch consolidation.
External prerequisites checked: Bubblewrap 0.11.2 functional enforcement on current host; final3 Pranikah guard unchanged.

### RESOLVED — Bubblewrap floor mismatch

**Current observed:** `/usr/local/bin/bwrap` → `bubblewrap 0.11.2`, uid/gid `0:0`, regular mode `0755`  
**Required floor:** `>=0.11.2`

Actions/evidence:

1. Version/ownership/mode readiness is resolved and freshly reverified.
2. Upstream build provenance: tag `v0.11.2`, peeled commit `1b80120ef26a28e065e67f89bfef873f13bdd317`, `support_setuid=false`.
3. Tasks 12 and 22 ran the full functional namespace/NoNewPrivs/capability/HOME/other-workspace/root-FD/network probes; the final run also passed `cargo test -p kodegpt-sandbox` 7/7.
4. Sandbox `ENFORCED` status is therefore supported empirically on this acceptance host rather than inferred from version metadata.

### RESOLVED — Narrow AppArmor policy enables trusted Bubblewrap without global userns relaxation

**Resolution:** the current Ubuntu/AppArmor host keeps `kernel.apparmor_restrict_unprivileged_userns=1`, but now has a dedicated root-owned site profile `/etc/apparmor.d/kodegpt-bwrap-userns-restrict` attached only to trusted `/usr/local/bin/bwrap`. The profile follows the Bubblewrap-specific pattern of allowing Bubblewrap's namespace setup while transitioning its executed child into a profile that denies capabilities.
**Evidence:** the profile passes `apparmor_parser -Q -K`, reloads successfully with `apparmor_parser -r`, and root `aa-status` lists both `kodegpt_bwrap` and `kodegpt_unpriv_bwrap` in enforce mode. Generic `unshare -Ur true` still fails writing `uid_map`, so unrestricted unprivileged user namespaces were not enabled globally. With the site profile active, `cargo test -p kodegpt-sandbox` passes all 7 tests including the complete functional isolation probe chain, and `cargo test --workspace` passes.

Required handling going forward:

1. Keep the global unprivileged-userns restriction enabled; the narrow Bubblewrap profile is an acceptance-host prerequisite, not a product/runtime dependency.
2. Never remove the locked `--unshare-user`, `--disable-userns`, `--assert-userns-disabled`, namespace, capability, or fail-closed requirements merely to accommodate another host.
3. Packaging/clean-install documentation must preserve this host prerequisite rather than silently altering system-wide userns policy.
4. Re-run the same mandatory functional probes on any different final acceptance/release host; the current PASS is host-specific empirical evidence, not a universal environment claim.

### RESOLVED — GitHub remote had no branch head

Actions/evidence:

1. Durable remote branch `feat/kodegpt-v0.1-execution-wt` has been published.
2. `origin` remains the expected `https://github.com/2ndworld/kodeGPT.git`; successful publication used SSH only as a non-interactive transport for this push.
3. Branch publication is not a product release/tag; the Task 16–25 worktree delta was durably committed in 17b6cf5 and pushed to origin.

### RESOLVED — Task 16–25 durably committed and pushed

Actions/evidence:

1. Task 16–25 implementation changes were durably committed in `17b6cf5` (`chore: complete kodegpt v0.1 release-candidate gates`) and tracker updated on branch `feat/kodegpt-v0.1-execution-wt`.
2. The branch head was successfully pushed to `origin` (`https://github.com/2ndworld/kodeGPT.git`) without force.
3. All 11 committed-state release gates were rerun post-commit and passed clean.

### RESOLVED — Repository-wide Rust formatting baseline

Repository-wide Rust formatting was applied in the worktree and `cargo fmt --all -- --check` now exits 0. Because source changed, the entire final3 guarded release checklist was rerun afterward: Rust workspace/security/acceptance/package/benchmark gates all remained green and the enclosing Pranikah compare returned `guard unchanged`.

### RESOLVED — GitHub Actions CI KodeGPT CI & AppArmor Enforce-Mode Security

Actions/evidence:

1. Canonical AppArmor enforce-mode policy (`kodegpt_bwrap` and `kodegpt_unpriv_bwrap` with `flags=(attach_disconnected)` and `audit deny capability`) and strict userns assertions were provisioned in `.github/workflows/ci.yml`.
2. `tests/integration/ci-contract.test.ts` was strengthened to enforce TDD RED/GREEN discipline against any non-enforce flags or capability-granting child profiles.
3. The feature branch `feat/kodegpt-v0.1-execution-wt` was pushed to remote (`https://github.com/2ndworld/kodeGPT.git`) head commit `a443d6b797ef44b706c4b2203cf3eb3ec03ffea3`.
4. GitHub Actions workflow `KodeGPT CI` run `31362216335` completed with 100% **`SUCCESS`**, passing all 19 job steps including Bubblewrap AppArmor enforcement and all local/CI deterministic v0.1 gates.

### PARTIAL — Actual ChatGPT host evidence

Real ChatGPT-host acceptance on 2026-08-12 has now observed KodeGPT tool discovery, opening a locally trusted workspace, read access, successful `file.edit` mutation/readback/exact-revert with clean Git, process calls reaching KodeGPT including expected policy denial, and `skill.list` / `skill.inspect` / `skill.load` action exposure reaching KodeGPT. This closes the earlier host action-exposure blocker for file mutation. MCP Apps visual rendering and a positive host `skill.list -> skill.inspect -> skill.load` round trip against a configured non-empty skill catalog remain separate unobserved categories; do not infer either from capability metadata. Granular evidence reconciliation is planned in the capability-quality follow-up below.

### NEXT IMPLEMENTATION PLAN — Capability Quality & Contract Reconciliation

Real-project host acceptance also exposed a quality gap: high-level `workspace.inspect`, `code.search`, and `context.build` spend bounded evidence/context budget on generated/vendor/VCS/worktree trees such as `node_modules`, `.git`, `.worktrees`, and `target`; `verify.list` also misses nested first-party package scripts in a multi-project repository. The repository additionally has factual contract/document drift around hybrid-skill semantic surface `0.2` vs the reconciled `0.3` plan and the missing `skill.list.compatibility` filter.

Authoritative follow-up design and execution plan:

- `docs/superpowers/specs/2026-08-12-kodegpt-capability-quality-reconciliation-design.md`
- `docs/superpowers/plans/2026-08-12-kodegpt-capability-quality-reconciliation.md`

Status: **IN EXECUTION on `feat/capability-quality-reconciliation`**. Tasks 1–6 have implemented semantic retained-root scope, high-level repository relevance, bounded nested verification discovery, `skill.list.compatibility`, semantic MCP surface `0.3`, and current-contract/document reconciliation; Task 7 is reconciling host evidence granularity. Provider interoperability (`provider.list`, `provider.tools`, `provider.invoke`) remains genuinely deferred future work requiring a separate design phase; this branch does not implement `skill.run`, provider invocation, generic tunnel abstraction, or desktop/computer-use authority.

### P1 — Security evidence must remain behavior-backed

Tasks 2–25 now contribute executable protocol, audit, trust, retained-FD workspace I/O, sandbox, process, Git, artifacts, Apps, extension, packaging, and full-stack acceptance evidence. The final local suite includes mandatory functional Bubblewrap probes and kernel-death/fallback regressions rather than scaffold-only success. Future changes must preserve the same RED/GREEN discipline and must not replace functional evidence with type/stub or skipped-host claims.

### P1 — Source-of-truth durability

The attached Master Blueprint is the product authority for this review. The repository should keep a durable copy/reference under `docs/architecture/` before long-running multi-agent work, so future agents do not reconstruct locked decisions from summaries alone.

## 7. Batch gate dashboard

| Batch | Tasks | Entry condition | Exit gate | Current state |
|---|---|---|---|---|
| A | 1–4 | All global execution-readiness preconditions satisfied | A2 | PASS — Tasks 1–4 DONE with fresh A2 review |
| B | 5–9 | A2 accepted | B1 | PASS — Tasks 5–9 DONE; fresh combined B1 workspace-boundary review accepted |
| C | 10–13 | B1 accepted | C2 | PASS — Tasks 10–13 DONE; fresh combined connector-auth/MCP/root-FD sandbox/trusted-executable/functional-isolation/raw-spool review accepted |
| D | 14–18 | C2 accepted | D1 | PASS — Tasks 14–18 DONE and durably committed in 17b6cf5 |
| E | 19–20 | D1 accepted | E | PASS — Tasks 19–20 DONE and durably committed in 17b6cf5 |
| F | 21–23 | E accepted | E2 | PASS — Tasks 21–23 DONE and durably committed in 17b6cf5 |
| G | 24 | E2 accepted | F1 | HOST PARTIAL — local isolation remains PASS; real ChatGPT discovery/read/write-edit/process/skill-action exposure observed on 2026-08-12; Apps rendering and positive non-empty skill round trip still pending |
| H | 25 | F1 accepted | FINAL | RC BEHAVIOR/QUALITY PASS for the historical v0.1 gate set; new capability-quality follow-up is PLANNED from real-project acceptance findings before final release/tag reconsideration |

**Rule:** never start work across a named gate using stale evidence. A gate is a reviewer stop, not a progress label.

## 8. Required evidence per task

Every task update must add or refresh all applicable fields:

```text
Task:
Status:
Commit(s):
RED command + expected failure:
GREEN command + result:
Full task verification command + result:
Security regression(s):
Changed files reviewed:
Blueprint requirements covered:
Known deviations: none | explicit approved amendment
External prerequisites checked:
Gate decision: PASS | REJECT | NOT YET REVIEWED
Reviewed at:
```

Do not mark `DONE` from an old green run after code changes.

## 9. Progress accounting rule

Avoid percentage-by-file-count. Use task/gate completion:

- Foundation credit is earned only after A1/A2 evidence.
- Security-sensitive tasks earn no completion credit from stubs, types, or mocked success alone.
- Batch completion requires the named review gate.
- v0.1 completion requires Tasks 21, 22, 23, 24 isolation, and 25 with fresh evidence on the intended acceptance host.

At this snapshot, the honest project state is:

```text
The historical Tasks 1–25 implementation/release-candidate gate set is durably implemented and its recorded local behavioral/security/quality verification remains evidence for that exact historical state.
Real ChatGPT-host acceptance on 2026-08-12 now observes discovery, locally trusted workspace open/read, successful file.edit mutation/readback/revert, process calls reaching KodeGPT, and skill action exposure; Apps rendering and a positive non-empty skill list/inspect/load round trip remain unobserved categories.
Real-project acceptance exposed a post-merge quality/contract follow-up: semantic repository scope for inspect/search/context, nested first-party verification discovery, skill surface 0.3/filter reconciliation, and stale-document/host-evidence reconciliation. The paired 2026-08-12 capability-quality plan is now in execution on `feat/capability-quality-reconciliation`; Tasks 1–6 are implemented and Task 7 reconciles host evidence before the final release regression gates.
Provider interoperability remains separately deferred; no provider invocation or skill.run is part of this follow-up.
```

## 10. Next action sequence

1. [DONE] Preserve the historical Tasks 1–25 release-candidate evidence and post-merge `main` baseline; do not replay completed feature-branch work merely because historical plan checkboxes are stale.
2. [DONE/PARTIAL HOST] Real ChatGPT host acceptance has observed discovery/read/write-edit/process/skill-action exposure. Keep Apps rendering and positive non-empty skill round-trip evidence granularly pending until actually exercised.
3. [IN EXECUTION] Execute `docs/superpowers/plans/2026-08-12-kodegpt-capability-quality-reconciliation.md` on `feat/capability-quality-reconciliation` using its TDD/review gates; Tasks 1–6 are complete and Task 7 host-evidence reconciliation is the current step.
4. [NEXT] Rerun full release verification and real-repository regression, record exact fresh evidence, and only then reconsider final release/tag claims.
5. [DEFERRED SEPARATE DESIGN] Provider interoperability (`provider.list`, `provider.tools`, `provider.invoke`) requires its own future security/design plan and MUST NOT be started as part of step 3.

Historical v0.1 execution mechanics remain in `docs/superpowers/plans/2026-08-09-kodegpt-v0.1-detailed-execution-plan.md`; the authoritative next-phase mechanics are the 2026-08-12 capability-quality reconciliation plan above.
