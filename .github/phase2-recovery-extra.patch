diff --git a/packages/mcp-server/src/structured-results.test.ts b/packages/mcp-server/src/structured-results.test.ts
--- a/packages/mcp-server/src/structured-results.test.ts
+++ b/packages/mcp-server/src/structured-results.test.ts
@@
     preview: {
       start: async () => ({} as never),
       inspect: async () => ({} as never),
       stop: async () => ({} as never)
     },
+    browser: {
+      openPreview: async () => ({} as never),
+      inspect: async () => ({} as never),
+      click: async () => ({} as never),
+      type: async () => ({} as never),
+      screenshot: async () => ({} as never),
+      console: async () => ({} as never),
+      networkFailures: async () => ({} as never),
+      releasePreview: async () => undefined,
+      releaseWorkspace: async () => undefined
+    },
     artifact: {
diff --git a/apps/cli/src/service/runtime-status.ts b/apps/cli/src/service/runtime-status.ts
--- a/apps/cli/src/service/runtime-status.ts
+++ b/apps/cli/src/service/runtime-status.ts
@@
-  surfaceVersion: "0.3" | "0.4" | "0.5" | "0.6" | "0.7" | "0.8" | "0.9" | "0.10" | "0.11";
+  surfaceVersion: "0.3" | "0.4" | "0.5" | "0.6" | "0.7" | "0.8" | "0.9" | "0.10" | "0.11" | "0.12";
@@
     value.surfaceVersion !== "0.9" &&
     value.surfaceVersion !== "0.10" &&
-    value.surfaceVersion !== "0.11"
+    value.surfaceVersion !== "0.11" &&
+    value.surfaceVersion !== "0.12"
   ) {
diff --git a/apps/cli/src/commands/service.test.ts b/apps/cli/src/commands/service.test.ts
--- a/apps/cli/src/commands/service.test.ts
+++ b/apps/cli/src/commands/service.test.ts
@@
                 host: "127.0.0.1",
                 port: 43_121,
                 protocolVersion: "2026-07-28",
-                surfaceVersion: "0.11",
+                surfaceVersion: "0.12",
                 runtimeVersion: "0.1",
